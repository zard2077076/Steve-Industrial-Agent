# Test matrix

Dated evidence catalog: counts below belong to their recorded inputs/commits, not
fixed assertions for new suites. Choose relevant behavioral/physical coverage and
record current results in `PROJECT_STATE.md`; do not treat every historical pending
row as a blocker for independent development.

| Read-only factory diagnosis A-DIAG-01 | PASS (server/core; client HUD not claimed) | `scripts/test-factory-diagnostic-gametest.sh`, focused core diagnostic tests | Six injected health categories map to stable fault/action/evidence records; healthy, fault and UNKNOWN are distinct; unknown and slow-progress status are not guessed into faults; reports cannot carry world mutation or automatic-repair authority. The isolated Forge 47.4.10/Create 6.0.6 gate executes both registered commands and passes 2/2: repeated player-project diagnosis preserves source block, exact chest stack, workflow and material ledger, while repeated warehouse diagnosis preserves order, runtime registration and world and classifies `MOLD_INSTALLED_NOT_POWERED` as `ENERGY_INSUFFICIENT`. `worldMutations=0`, container/ledger/order/runtime mutations 0, crash reports/residual processes 0; `work/logs/factory-diagnostic-gametest-20260810-014836.log`. |

| Phase IV-C player workflow | IMPLEMENTED, in-world visible pending | Focused material/player tests, aggregate JVM suite, `scripts/Test-SitePreparationGameTest.ps1` / `scripts/test-site-preparation-gametest.sh`, integrated 22-test server, userdev startup | Terminal/preview/relocation/approval and exact material-source/reservation/journal codecs and persistence pass. Core + Adapter + Forge are 640/640; the former 22 macOS formal-world fixtures now pass by expecting safe canonical-path/alias refusal without weakening production guards. Site Preparation is 12/12, including stable UUIDs through a 60-tick identical-drop merge window and exact Bot delivery. The 59/59 target gate proves a searched non-reviewed `blue_concrete` can create a player project. The fresh integrated server is 22/22 with all dimensions saved and no crash report. Selected-source Direct/Bot/Hybrid material movement, exact cancel/return, explicit salvage transfer and balanced completion reporting are implemented. The new Mac player-operated source-selection/courier/report click-through remains pending and is not inferred from automated evidence. |

| Phase V/VI industrial foundation | PARTIAL PHYSICAL, production mutation gated | `-PieAdapterAcceptance`, `-PiePhysicalAcceptance`, aggregate JVM suite | IE 10.2.0-183 read-only discovery proves 24 Metal Press recipes, seven template components and a real LV wire edge with zero adapter mutations. The separate disposable physical fixture proves real formation, mold interaction, iron input, 2,400-FE consumption, iron-plate output and exact restoration in 61 ticks. Warehouse orders round-trip their complete allowlist/in-flight state and tick only through a re-registered runtime. Explicit Forge fluid endpoints pass delivery, zero-withdrawal capacity refusal and drift reclaim/return. Player-facing IE `execute`, live FE source wiring, physical inter-factory routes and pipe/pump topology remain gated. |
| IE-MP-02 item-fuel planning and Alloy Smelter adapter | PASS prerequisite; transactional player order pending | Focused industrial JVM tests and `scripts/test-ie-physical-acceptance.sh` | `IndustrialPowerRequirement` makes FE-network and item-fuel authority mutually exclusive. The existing Metal Press still verifies FE; a two-batch Alloy Smelter plan carries two exact coal material rows, 400 burn ticks, no FE and no electrical graph, and requires both an item-input port and HEAT capability. The version-pinned adapter binds the live brass recipe, discovers real input/fuel/output slots, inserts one coal, observes its consumption and the unique two-ingot output, claims only the real stack and leaves zero internal residue. The eight-cell baseline and the existing full Metal Press ledger/FE flow both restore/pass in `work/logs/ie-physical-acceptance-20260810-132242.log`; the 23-task/854-test clean build passes in `work/logs/macos-clean-build-ie-mp02-fuel-foundation.log`. This does not yet claim player-source reservation, Bot transport, reload settlement, report or warehouse dispatch for the Alloy Smelter. |

| Phase IV integrated automated acceptance | PASS (visible gate pending) | Site/fleet plus all Phase IV isolated Forge suites, `scripts/Test-PhaseIVIntegratedVisibleGameTest.ps1`, `scripts/Test-Core.ps1`, `scripts/Test-GoalDrivenExecution.ps1`, `scripts/Build.ps1` | The exact PlacementAnchor-to-real-C-07 chain plus explicit slope/hole grading and destructive second-confirmation fixtures pass 10/10 with the shared Steve/Alex terrain fleet (`site-preparation-gametest-20260728-160309`). The player command allowlists exact C-05-through-C-10 goals only behind the fresh prepared-site bridge. The visible-equivalent registration passes 22/22 across that bridge, all six three-mode goals, 3/5 Bot, Composite-01/02/03 and bounded C-10 expansion (`phase-iv-integrated-visible-gametest-20260728-160739`). Shared recovery/cancel 10/10, faults 5/5, C-10 safety 10/10, Composite faults 3/3 and full registered goal execution 42/42 remain green. Latest loader-neutral regression is 550/550 across 129 suites (`core-test-20260728-160532`); final clean build is 23/23 tasks and 583/583 aggregate XML tests with artifact isolation (`build-20260728-161519`). No PCL2 instance was launched after this change; final source-bound JAR deployment and one backed-up new-region player-visible acceptance remain. |
| Phase IV C-05 real crushing | PASS (automated checkpoint) | `scripts/Test-C05Crushing.ps1`, `scripts/Test-Core.ps1`, `scripts/Test-GoalDrivenExecution.ps1`, standard and DeceasedCraft runtime probes | 4/4 C-05-only GameTests and 22/22 aggregate GameTests prove one verified opposed-wheel plan across Direct/two active Bots/Hybrid, real gravel input and chest sand output, probabilistic byproduct observation, exact BUILD recovery with no duplicates, cleanup, typed material/formal/obstruction/cancel/wrong-direction/foreign-output/lost-power failures, and no crash/residual process. Standard runtime: 2,609 total, crushing 55 discovered/12 mapped. Disposable DeceasedCraft: 18,286 total, crushing 103, selected safe raw-copper recipe, external mutation/save read false. Final review reruns: `core-test-20260724-084358`, `c05-crushing-gametest-20260724-084508`; full evidence: `goal-driven-execution-gametest-20260723-203921`, `create-runtime-recipe-catalog-20260723-201435`, `deceasedcraft-isolated-server-20260723-203557`. |
| Phase IV C-06 through C-10 shared runtime gate | PASS (WIP automated gate) | `scripts/Test-PhaseIVCapabilities.ps1`, `scripts/Test-PhaseIVRecovery.ps1`, `scripts/Test-PhaseIVFaultMatrix.ps1`, `scripts/Test-Core.ps1`, `scripts/Build.ps1` | 5/5 live-recipe Direct/Bots/Hybrid equivalence, 10/10 exact BUILD-prefix recovery plus three-mode cancellation, and 5/5 concentrated fault matrix pass. Evidence proves real selected outputs, exact multi-stack consumption, two active/moving non-overlapping Bots, Hybrid provenance, no teleport/fixed-sleep/product synthesis, zero duplicate placement/input/output after checkpoint round-trip, exact cancellation return, formal-world refusal, obstruction/navigation/orientation/power faults and ownership-aware cleanup. The shared capability suite remains 5/5 after the fleet refactor in `phase-iv-capability-gametest-20260727-165442`. Prior full regression is 456 core + 55 Adapter + 23 Forge, 23/23 clean-build tasks and artifact isolation; C-05 post-build regression remains 4/4. Product rows remain IN PROGRESS until Site Preparation integration and one visible acceptance. |
| Phase IV Composite-01 | PASS (WIP automated gate) | `scripts/Test-PhaseIVComposite.ps1`, focused `CompositeProductionCoordinatorTest`, `scripts/Test-Core.ps1` | 6/6 loader-neutral graph/buffer/recovery/shared-lease tests pass. Direct/Bots/Hybrid each run three real v606 stages and two locked-Hopper intermediate routes, producing one real cogwheel while physically preserving five unreserved planks in salvage. Same graph fingerprint, exact route conservation, visible Bot movement, per-stage reload and full owned machine/route cleanup pass in the final 9/9 combined run: `phase-iv-composite-gametest-20260727-171847`. |
| Phase IV Composite-02 three-mode cancellation isolation | PASS (WIP automated gate) | `scripts/Test-PhaseIVComposite.ps1`, focused `CompositeProductionCoordinatorTest` and `SharedInfrastructureLeaseRegistry` JVM coverage | Real concurrent C-07 and C-08 lines have disjoint verified plans, graphs, targets, sessions and worker identities. In Direct/Bots/Hybrid, cancelling A restores its exact source/delivery state but leaves B's sole shared-interlock reference and physical lock intact. Handler reconciliation then gates a wrapper snapshot/restore that preserves A cancelled, converts B to `RECOVERY_REQUIRED`, preserves the B-only lease/interlock and refuses mismatched evidence before exact graph/node/generation/reservation evidence resumes B. B produces one real andesite alloy; final release removes the lock, Hopper delivery completes and cleanup leaves no owned blocks. The final combined run passes 9/9 with clean save/exit and no residual Java: `phase-iv-composite-gametest-20260727-171847`. |
| Phase IV Composite-03 three-mode branch/merge | PASS (WIP automated gate) | `scripts/Test-PhaseIVComposite.ps1`, `scripts/Test-PhaseIVCompositeFaults.ps1`, focused loader-neutral branch/merge JVM coverage | One real four-node/five-edge DAG physically splits exact branch inputs, runs independent C-07 plank and C-10 cogwheel branches, holds merge until both succeed, routes one plank plus the cogwheel into C-10 and produces one real large cogwheel. Direct/Bots/Hybrid pass with existing Steve/Alex movement and exact Hybrid route provenance; typed inventory/Hopper filters, per-handler reload, salvage and cleanup pass in the final 9/9 run `phase-iv-composite-gametest-20260727-171847`. The separately isolated 3/3 fault run `phase-iv-composite-fault-gametest-20260727-172021` proves exact contamination, merge backpressure and branch-input-loss refusal, sibling isolation, reconciliation and cleanup. |
| Phase IV C-09 compacting bounded fluid gate | PASS (visible acceptance pending) | C-09 core/Adapter/Forge tests plus `scripts/test-goal-driven-execution.sh`, registry survey, Composite player-order/recovery and warehouse/restart gates | Contract is counted multi-item with exact optional FLUID inputs, deterministic item output, NONE heat and remains distinct from C-04. Real `granite_from_flint` consumes one whole lava bucket, injects/journals exactly 100mB, matches the live recipe and leaves 0mB in the Basin. Direct/Bots/Hybrid share one plan and equivalent material/output/pre-resource-reload/cleanup evidence; pre-feed cancellation returns the exact bucket in all modes. Aggregate 63/63 passes in `goal-driven-execution-gametest-20260810-124736.log`. Survey maps 439 recipes/315 products with `plannerFailures=[]` while four fluid-stage Composite expansions remain typed refused; all four reviewed/derived Composite representatives pass 12/12 in `composite-player-order-20260810-123837.log`. Two-process resume and reload/cancel plus warehouse and restart pass in the `20260810-125034`, `125159`, `125308` and `125807` logs. The 23-task clean build passes 850 tests in `macos-clean-build-lava-compacting-final.log`. Chocolate/honey, fluid outputs, multi-cycle fluids, mid-feed resource recovery and HEATED remain closed. |
| Phase IV C-08 mixing bounded fluid gate | PASS (visible acceptance pending) | C-08 core/Adapter/Forge tests plus aggregate Goal-Driven gate | Contract covers counted concrete selections verified against live tag/any-of Ingredients, exact optional FLUID input, exact output, NONE and real ordinary-fuel KINDLED/HEATED. Real `create:pulp` consumes one whole water bucket, injects/journals exactly 250mB, matches the live recipe and leaves 0mB in the Basin. Unreviewed/bulk fluid, fluid output, SUPERHEATED, residue and unknown NBT remain typed refused. |
| Phase III executor three-mode | PASS | `scripts/Test-GoalDrivenExecution.ps1` | 18/18 required GameTests: C-03 and C-04 Direct/Bots/Hybrid share one verified plan/graph and match final snapshot, orientation, ports, shafts/belts, output, evidence, cleanup, idle reload, material consumption and zero failures; the two-process C-03 BUILD checkpoint resumes only trusted node 0 after exact rescan and produces zero duplicate placement/input/output; build/pilot mode command tree PASS; crash reports/residual processes 0; `work/logs/goal-driven-execution-gametest-20260723-164019.log` |
| Phase III committed-JAR visible recovery | PASS | Dedicated `SteveAgent-Executor-Test`, two independent client processes | JAR commit `eb5c3e4`, SHA-256 `F2346AC5...A65A06C`: C-03 initial BUILD reported `persisted=true`, normal exit saved all dimensions, re-entry reported `PERSISTED/BUILD/SAFE_CHECKPOINT`, resume passed exact rescan with zero duplicate placement/consumption/output, both process nodes completed gravel 3/3, cleanup removed 50 journal-owned blocks with workers 0 and unknown removals 0; crash reports/residual Java 0; `work/logs/iwp-visible-recovery-pre-reload-20260723-165924.log`, `work/logs/iwp-visible-recovery-post-reload-20260723-170450.log` |
| Phase III A/B integration | PASS | Ordered cherry-pick plus JVM/build/two isolated runtime suites | `d368158` skipped; frozen executor-package changes 0. Core 416 + Adapter 48 + Forge 12 = 476/476 with 23/23 tasks and artifact isolation. A runtime 18/18 preserves Direct/Bots/Hybrid equivalence and initial multi-process recovery. B runtime emits 40 tasks/56 dependencies, Bots unsupported, no executor created, and assignment/plan/runtime/world/delta-bound observers with no world mutation. Pure game imports, B executor declarations, blocking/network primitives, crashes and residual Java are all 0. Logs: `core-test-20260723-171042`, `build-20260723-171151`, `goal-driven-execution-gametest-20260723-171411`, `create-runtime-recipe-catalog-20260723-171710`. |

| Layer/profile | Current | Required evidence |
|---|---|---|
| Public Alpha metadata/privacy | PASS | Version metadata, release JAR boundary, config precedence/refusal, tracked/history secrets, private/large files and public-path scans pass. Latest privacy scan has zero high-confidence current/history secret matches, zero tracked private names and zero tracked files over 5 MiB. Latest local build passes 23 tasks and 418 XML-reported test cases; the exact final artifact hash is recorded only in the generated staging manifest. |
| Public Alpha diagnostics | PASS | Exact compatible and incompatible runtime decisions are unit tested; diagnostic facts are allowlisted and path/secret-shaped values redact. The deterministic ZIP test proves exactly one `diagnostics.txt` entry and no automatic upload. `work/logs/core-test-20260722-112426.log`. |
| Public Alpha clean-room build | CURRENT-HEAD OFFLINE A/B PENDING | One online cold-cache build passed at `3a85050`, recorded 363 dependency files and produced a 1,096-file immutable read-only mirror; the first isolated offline A/B was byte-identical. Current source-only fixes leave all Gradle/wrapper dependency inputs unchanged, so the same verified mirror may be reused without another public cold download. Final A/B evidence must bind the replacement commit. |
| Public Alpha sole-JAR install/server | PASS | A fresh synthetic install contains only the release JAR, matches SHA-256, includes commands/metadata and contains no personal path. Official Forge 47.4.10 with neither optional mod loads that sole JAR and exits cleanly: `work/logs/packaged-neither-server-20260722-111808.log`. |
| Public Alpha physical/recovery regression | PASS, REPLACEMENT VISUAL RC CHECK PENDING | The A01 candidate run passed setup/backup/restart and safe preview, then failed closed with `worldMutation=false` on a disjoint-backup policy defect and off-center two-module layout. Both are fixed. The 418-test build, 14 goal-driven GameTests and both two-process recovery fixtures pass with no duplicate output or crash; the committed replacement JAR still needs the complete human-visible gravel/iron-sheet lifecycle before RC1. |
| Core values/geometry | PASS | Clean JUnit results for IDs and rotations |
| World environment classification | PW-01 PASS | Eight pure JVM tests prove allowed disposable classification, forbidden-root precedence, unknown-by-name refusal, path-traversal normalization, unresolved-real-path refusal, actual symlink/verified-alias canonicalization, gameDir/world-root mismatch and immutable bounded evidence. Dangerous descriptors cannot carry write/execution authority. Full 240-test suite and clean 23-task build pass in `work/logs/core-test-20260717-182831.log` and `work/logs/build-20260717-182939.log`; no save/world runtime was opened. |
| Deployment policy | PW-02 PASS | Five pure JVM tests enforce the dry-run-only formal default, zero formal mutation, no automatic withdrawal/base reuse, protected unknown/player/BlockEntity state, formal/unknown/forbidden execution construction refusal, bounded mutually consistent budgets, immutable allowlists, explicit approval/time bounds and forbidden-root precedence. Full 245-test suite and 23-task build pass in `work/logs/core-test-20260717-183444.log` and `work/logs/build-20260717-183527.log`. |
| Formal-world write guard | PW-03 PASS | Three JVM methods cover all seven write entry classes, formal/unknown/forbidden priority and approval/backup/snapshot/region/budget gates. A real twelfth goal-driven GameTest revokes expected-gameDir after session construction and proves handler typed refusal plus zero target mutation in `work/logs/goal-driven-execution-gametest-20260717-193003.log`. C-02/C-03/C-04 and both production reload paths pass under per-write real-path/marker checks; 248 tests and 23-task build pass in `work/logs/core-test-20260717-192143.log` and `work/logs/build-20260717-192424.log`. |
| Deterministic deployment preview | PW-04 PASS | Six pure JVM methods cover the full verified-chain projection, every required preview section, canonical JSON, same-input/map-order determinism, content and independent snapshot hash changes, SHA-256 self-validation, protected BlockEntity/container evidence, formal preview-only violations and reflection-based exclusion of game/session/runner/resource authority. Final 254-test and 23-task logs are `work/logs/core-test-20260717-201549.log` and `work/logs/build-20260717-201640.log`; fresh C-02/C-03/C-04, 12-goal, two recovery and final-JAR DeceasedCraft isolated logs are `201759`, `201920`, `202036`, `202120`, `202249`, `202504`, `202714`. |
| Deterministic deployment risk | PW-05 PASS | Three pure JVM methods exercise all 22 required categories in enum order, every INFO/LOW/MEDIUM/HIGH/CRITICAL severity, map-order determinism, safe INFO-only output, CRITICAL approval blocking and reflection-based loader/LLM exclusion. Constructor invariants prevent a CRITICAL finding from becoming non-blocking or a lesser finding from claiming the critical gate. Final 257-test and 23-task logs are `work/logs/core-test-20260717-204229.log` and `work/logs/build-20260717-204309.log`. |
| Deployment resource and power budget | PW-06 PASS | Four pure JVM methods cover raw/intermediate/construction/power/logistics/input/output quantities, stress and power margin, expected/maximum ticks, policy affected-block maximum, rollback/journal space, all 11 canonical violations, exact six-value resource-source policy, formal no-withdraw/read-only semantics and loader/resource-buffer exclusion. Final Test-Core and 261-test/23-task build evidence is `work/logs/core-test-20260717-210025.log` and `work/logs/build-20260717-210107.log`. |
| Exact region authorization | PW-07 PASS | Five pure JVM methods cover all 13 operations, exact world/environment/dimension/optional-owner/scope/mutation/preview/snapshot/runtime matching, expiry, approval, revocation and one-time-use state. Formal/unknown/forbidden records cannot be approved or exceed read-only/zero-mutation modeling, and a formal check always fails live. Final Test-Core and 266-test/23-task artifact-isolated build evidence is `work/logs/core-test-20260717-210952.log` and `work/logs/build-20260717-214020.log`. |
| One-time human approval gate | PW-08 PASS | Six pure JVM methods cover exact structured approval, repeated and 16-way concurrent consumption, preview/world/snapshot/runtime/reload/region/target/quantity/mutation/policy drift, material/block budget expansion, pending/rejected/expired states and TEST_ONLY formal-copy prevention. The token has no boolean approval field and the gate cannot issue one. Final Test-Core and 272-test/23-task build evidence is `work/logs/core-test-20260717-214810.log` and `work/logs/build-20260717-214948.log`. |
| Isolated backup and restore foundation | PW-09 PASS | Six JVM methods use a real temporary world tree to verify canonical inventory/per-file SHA-256/manifest hash, size/headroom/live capacity, target/atomicity/consistency/restore/retention/failure/approval policy, staging creation, controlled modification, exact restore fingerprint and journal identity. Incomplete/corrupt/space/source/target/formal cases refuse before copy. Final Test-Core and 278-test/23-task build evidence is `work/logs/core-test-20260717-220320.log` and `work/logs/build-20260717-220506.log`. |
| Claim and permission Adapter boundary | PW-10 PASS | Five JVM methods cover complete operation/actor/region/world/source/time/generation/fingerprint query and attributed evidence, exact UNKNOWN/ALLOWED/DENIED/NOT_INSTALLED states, isolated allow, explicit deny, unknown/not-installed default refusal, formal claimed-allow refusal and non-hardcoded third-party implementation. Final Test-Core and 283-test/23-task build evidence is `work/logs/core-test-20260717-221447.log` and `work/logs/build-20260717-221609.log`; OPAC 0.25.8 discovery uses retained disposable evidence only. |
| Independent deployment readiness gate | PW-11 PASS | Pure JVM coverage proves all 25 checks on success, independently mutates each check to typed refusal, verifies the exact 35-code failure set and complete refusal context, rejects one-time approval replay and uses reflection to exclude sessions/executors/world/resource/mutation authority. The final suite is 307/307 in `work/logs/core-test-20260719-101624.log`; clean build and fresh C-02/C-03/C-04, goal, reload, standard-runtime and disposable DeceasedCraft acceptance pass in the 20260719 logs recorded in project state. |
| Read-only deployment commands | PW-12 PASS | Pure JVM tests enforce complete bounded formatting, shared preview identity, absence of game/session/executor/resource/LLM authority and construction-time refusal of formal execution authority. The real server command matrix covers preview/risks/budget/readiness, gravel x3, iron sheet x2, three orientations, invalid typed input and authoritative-thread refusal; `work/logs/create-runtime-recipe-catalog-20260719-115510.log`. The production JAR executes twelve matching custom milling/pressing dry-runs in the disposable 288-mod profile with no session/mutation and unchanged external fingerprints; `work/logs/deceasedcraft-isolated-server-20260719-115632.log`. The final 310-test/23-task and complete physical/recovery regressions are recorded in project state. |
| Formal save discovery | FS-01 PASS | Three pure JVM discovery tests prove a direct single save can be selected from guarded bounded metadata, multiple saves remain ambiguous even when LastPlayed and ranking differ, `level.dat_old` fallback works, missing/unreadable roots are typed, nested region files are never enumerated or handed to the reader, metadata mtime is unchanged and no `session.lock` is created. Final hardening evidence: `work/logs/core-test-20260720-091520.log`, `work/logs/build-20260720-091700.log`. |
| Formal-world read-only guard | FS-02 PASS | Six pure JVM tests prove callback-scoped READ-only channels; bounded, audited, non-recursive direct discovery; ignored external JSONL audit; unchanged source mtime; typed refusal for every create/write/append/delete/move/replace/attribute/ACL/lock/process intent; outside/private/reparse rejection; unsafe audit-root refusal; and deterministic manifest/key-SHA mutation detection. `FormalSaveDiscovery` has no filesystem API. Final 334-test/23-task evidence: `work/logs/core-test-20260720-091520.log`, `work/logs/build-20260720-091700.log`. |
| FS-12 fingerprint preflight hardening | PASS | Existing guard regression now proves guard-owned size/mtime, hash-all for every non-private ordinary file, private playerdata metadata inclusion with no READ_OPEN, exact mutation detection and unchanged source. 363 JVM/Adapter tests and 366 build-included tests pass in `work/logs/core-test-20260720-105957.log` and `work/logs/build-20260720-110101.log`. |
| FS-12 real bounded formal survey | PASS WITH EXPLICIT PARTIALS | Real acceptance enumerates 59 regions, bounded-scans 16 and parses 9,733 chunks; 29 undersized headers and 40 classification-bound chunks remain typed partial evidence. Eight machine-seeded candidates are UNKNOWN/PENDING only and four formal dry-runs are explicitly blocked/no-hash. Equal `00571b...a834a34` pre/post, unchanged external metadata, zero formal writes/private opens/session changes/crashes/processes. Final evidence: `formal-world-survey-20260720-115048.log`, `core-test-20260720-122205.log` (370), `build-20260720-122312.log` (373/23), C-02 `120003`, C-03 `120143`, C-04 `120247`, goal GameTest `120352`. |
| FB-01 formal backup destination policy | PASS | Seven JVM methods bind world/source/runtime/time/policy/tool/Git/capacity into deterministic non-executable plans; exact ignored-root, hierarchy, arbitrary/formal/reparse/space/collision refusals and complete B-11 vocabulary pass. No source open or target creation. `core-test-20260720-123333.log` (377), `build-20260720-123447.log` (380/23/artifact isolation). |
| FB-02 guarded formal-source backup transaction | PASS ON FIXTURES | Four JVM methods prove complete path/size/mtime/SHA-256 manifests, opaque private transport without parsing, explicit lock/temp exclusions, fresh structured quiescence refusal, unique reparse-safe staging, exact rehash, atomic publish, aggregate-only completion marker, equal source fingerprints/metadata and typed source-drift cleanup. No formal source was opened. `core-test-20260720-131013.log` (381), `build-20260720-131129.log` (384/23/artifact isolation). |
| FB-03 completed identity and disposable restore drill | PASS ON FIXTURES | Three JVM methods reconstruct exact backup identity from canonical persisted manifest/marker plus the exact expected plan, freshly verify all files, restore only below repository work, rehash before/after atomic publication and offline-reparse exact WorldIdentity. Arbitrary target and changed backup fail before restore; private bytes stay opaque and no process/formal source API is used. `core-test-20260720-133204.log` (384), `build-20260720-133309.log` (387/23/artifact isolation). |
| FB-04 formal deployment candidate packages | PASS | Three JVM methods prove deterministic multi-candidate ranking without selection, exact available preview/physical/risk/budget/world/backup binding, explicit real-style unavailable artifacts, UNKNOWN claim and missing-authorization retention, mismatch refusal and structural no-top-1/no-approval/no-execution. `core-test-20260720-134155.log` (387), `build-20260720-134304.log` (390/23/artifact isolation). |
| FB-05 approval request and formal hard stop | PASS | Three JVM methods prove real awaiting-selection requests have no scope, selected fixtures bind exact one-time world/preview/backup/snapshot/runtime/budget/operation scope, selected-ID/runtime drift fails, TEST_ONLY cannot enter formal scope, formal use is impossible, formal decisions remain ABSENT and the independent hard stop always returns `FORMAL_WORLD_EXECUTION_FORBIDDEN`. `core-test-20260720-134956.log` (390), `build-20260720-135108.log` (393/23/artifact isolation). |
| FB-06 registered management and privacy boundaries | PASS | Three JVM methods cover all seven B-10 commands, exact bounded identity registries, audit-only safe keys, path/traversal/unknown/extra/unregistered-verb refusal, no session/write/approval/inventory/execution, request-only bounded inventory with no contents and UNKNOWN claim non-readiness without private database or permission mutation. `core-test-20260720-135754.log` (393), `build-20260720-135914.log` (396/23/artifact isolation). |
| FB-07 real backup, verification and restore drill | PASS | One guarded real transaction copied 215 files/214,122,806 bytes, explicitly excluded the three-byte root lock, published manifest `0ded5c...b46c`, independently verified backup `formal-backup:dff7...deed` and offline-reparsed exact WorldIdentity from restore drill `restore-drill:63c0...eb93`. Source fingerprint `00571b...a34a34` is equal before/after; postcheck proves unchanged lock, zero Java processes/crashes. `formal-backup-acceptance-20260720-141225.log` and ignored aggregate report. |
| FB-08 candidate/approval close-out | PASS, PENDING ONLY | Eight candidate packages are preview/physical/risk/budget unavailable and `PENDING_USER_SELECTION`; eight requests are `AWAITING_USER_SELECTION` without scope; allowed decisions are zero, claim remains UNKNOWN and formal execution remains forbidden. Final evidence: core 393/build 396, C-02/C-03/C-04, 12/12 goal, Create-only 2,609-recipe dry-run and disposable 288-mod/18,286-recipe DeceasedCraft dry-run logs recorded in project state. No candidate selection, approval, inventory-content read or formal mutation. |
| Loader-neutral formal survey model | FS-03 PASS | Four pure JVM methods prove complete finding provenance, immutable deterministic world/dimension/region/chunk/zone output, exact confidence/failure vocabulary, pending-only candidates, equal-fingerprint completion, zero write/private-content/execution authority, offline live-state refusal and reflection-based loader/NBT exclusion. Full 322-test and 23-task evidence: `work/logs/core-test-20260719-130026.log`, `work/logs/build-20260719-130230.log`. |
| Bounded offline Anvil/NBT parser | FS-04 PASS | Six pure JVM methods prove bounded gzip `level.dat`; gzip/zlib/raw Anvil chunks; sector, overlap, length and coordinate validation; 4-bit plus Minecraft non-crossing 5-bit/342-long palette decoding and invalid-index refusal; location-only BlockEntity redaction; valid-sibling continuation; and typed depth/array/decompression/DataVersion refusal. Final 335-test JVM/Adapter and 23-task clean-build evidence: `work/logs/core-test-20260720-092449.log`, `work/logs/build-20260720-092548.log`. No formal region was read. |
| Deterministic bounded survey scheduler | FS-05 PASS | Six pure JVM methods prove explicit atomic region/chunk/read-byte/duration/retained-memory/candidate/deep-radius limits, exact wrong-thread refusal, deterministic metadata heat/neighbor/candidate ordering, candidate-only deep work, duplicate suppression, bounded input and first-exhaustion `SURVEY_PARTIAL` with unchanged counters and full failure context. Final 333-test JVM/Adapter and 23-task clean-build evidence: `work/logs/core-test-20260720-090130.log`, `work/logs/build-20260720-090333.log`. No formal path was read. |
| Offline Create/storage classification | FS-06 PASS | Four pure JVM methods classify all required power/transmission/processing/ITEM/fluid/moving families, generic Create states and exact/heuristic storage with deterministic exact positions and evidence. Storage remains type/location-only with `contentsRead=false`; heuristic modded storage is low-confidence; live RPM/stress/connectivity remain unknown; invalid context/sections are rejected. Final 339-test/23-task evidence: `work/logs/core-test-20260720-093235.log`, `work/logs/build-20260720-093334.log`. |
| Offline industrial topology approximation | FS-07 PASS | Five pure JVM methods prove deterministic same-dimension unit adjacency for rotational power, ITEM and fluid edges; axis-backed derived-high confidence; low-confidence processing/storage attachments; runtime-connectivity refusal; explicit partial boundaries; duplicate/gap rejection; and both-source provenance across region boundaries. Final 344-test JVM/Adapter and 23-task clean-build evidence: `work/logs/core-test-20260720-100242.log`, `work/logs/build-20260720-100340.log`. |
| Formal candidate-zone scoring | FS-08 PASS | Five pure JVM methods prove fixed eight-factor scoring, infrastructure distance derived from exact retained positions, deterministic SHA-256 identities/order/ties, canonical anchors/findings/limitations, UNKNOWN permission and partial/protected authorization requirements, bounded/duplicate/inconsistent-input refusal and pending-only output. Final 349-test JVM/Adapter and 23-task clean-build evidence: `work/logs/core-test-20260720-101700.log`, `work/logs/build-20260720-101758.log`. |
| Claim/permission metadata discovery | FS-09 PASS | Five pure JVM methods prove known claim-mod/version/hint discovery, deterministic multi-mod ordering, conflict and 128-document bounds, exact relative-source/key allowlists, credential-shaped value and remote/private database refusal, Adapter-backlog-only output and structural UNKNOWN/no-access/no-modification/no-execution results. Final 354-test JVM/Adapter and 23-task clean-build evidence: `work/logs/core-test-20260720-102659.log`, `work/logs/build-20260720-102802.log`. |
| Formal four-candidate dry-run batch | FS-10 PASS | Four pure JVM methods require exact standard quantities and distinct pack recipe classes, pending zone/dimension/in-bounds anchor binding, formal valid unique preview hashes, equal runtime/snapshot, UNKNOWN permission, explicit execution refusal and zero inventory/resource/session/approval/execution authority. Final 358-test JVM/Adapter and 23-task build: `work/logs/core-test-20260720-104630.log`, `work/logs/build-20260720-104725.log`. |
| Registered formal survey commands | FS-11 PASS | Five pure JVM methods cover list/inspect/infrastructure/candidates/preview grammar, registered identities and targets, bounded quantity/registries, deterministic ignored report keys, typed path/traversal/unknown/extra-argument failures and zero filesystem/process/session/write/inventory/approval/execution authority. Final 363-test JVM/Adapter and 23-task build: `work/logs/core-test-20260720-105255.log`, `work/logs/build-20260720-105407.log`. |
| Generic resource types | PASS | Exact six loader-neutral categories, stable unique serialized names, round-trip parsing and invalid-input rejection |
| Unified machine graph | PASS | Seven invariant/topology tests plus G-09 production-plan tests enforce bounded immutable nodes/ports/edges. C-03 carries its six-node relative graph and C-04 its nine-node relative graph in their production generic plans. No automatic layout or routing is claimed. |
| Typed anchor and plan transform | PASS | Five G-13 pure JVM tests cover all four Y quarter turns, coordinate overflow, block and machine axes, facing, machine orientation, port sides, complete C-03/C-04 plans and unchanged graph/node/port/edge identities. Physical C-03/C-04 GameTests each pass ZERO, CLOCKWISE_90 and CLOCKWISE_270; 180 degrees is JVM-verified but not physically claimed. No mirror or layout search is implemented. |
| Basic placement feasibility | PASS | Five G-14 pure JVM tests cover clear C-03/C-04 final targets at all four rotations, exact unique observation coverage, bounds/immutability, deterministic complete position reports and `UNLOADED`/`NOT_REPLACEABLE`/`PROTECTED`/`INTERNAL_ROLE_CONFLICT`. Each physical C-03/C-04 orientation rejects four obstacle/protection conflicts at three final positions with zero target mutation before retaining its real processing pass. The gate does not search alternatives. |
| Typed production goal | PASS | Five P-02 pure JVM tests cover an immutable ITEM target/quantity, bounded optional mod/depth/material/preference/owned-resource constraints, conflict rejection, canonical insertion-order-independent codec round-trip and corrupt/truncated/trailing/oversized frame refusal. It is input data only and has no world, execution or LLM authority. |
| Deterministic recipe catalog | PASS | Four P-03 pure JVM tests cover immutable bounded recipe identity/type, typed input/output/optional-byproduct quantities, required capabilities/resource types, optional duration, source adapter/mod and runtime fingerprint/verification state. Catalog and output candidates are canonically ordered; duplicate IDs/resources, output/byproduct overlap, incomplete type declarations and invalid source metadata fail closed. No runtime Create recipe discovery is claimed. |
| Create runtime recipe, capability, logical planning and command | R-01 THROUGH R-08 PASS | Runtime boundary, entry, ingredient, codec, request-resolution, capability, orchestration and command-format tests enforce typed failures/warnings, immutable snapshots, exact/any-of/tag/unsupported identity, stable owned/canonical selection, provenance, fingerprint guards, ceiling batch conversion and complete verification. The isolated server reads 2,609 actual recipes, accounts for 44 milling plus 7 pressing as 32 mapped or 19 typed limitations, publishes two capabilities, verifies real gravel/pressing plans and executes the read-only typed command success/failure paths. Invalid ID/quantity are parser-rejected, off-thread and no-Create calls fail typed, reload rebuilds fingerprints, the probe block is preserved and no execution session is created. R-08 keeps formal-pack file differences separate from unverified pack runtime claims. |
| DeceasedCraft isolated profile | R-09A through R-09F + LP-12 PASS | The safe repeatable profile and production-JAR launch retain all path/hash/process guarantees. The authoritative isolated RecipeManager reports 18,286 recipes; 66 milling plus 37 pressing account as 82 mapped plus 21 typed rejections. Four pack-backed plans pass eight planning and fifteen binding checks, then each physicalizes in three orientations for 12 thirteen-check `VerifiedPhysicalPlan` values. Seven layout failures are exact, sequenced assembly remains rejected, and no session/world mutation/external write occurs; `work/logs/deceasedcraft-isolated-server-20260717-015152.log`. |
| Machine implementation descriptor and logical ports | IB-02/IB-03 PASS | Five pure JVM tests cover stable real identity, Adapter/capability/family/recipe/resource/runtime attribution, exact nine-role logical ports, type/mode/throughput/connection/evidence semantics, continuous power consistency, physical-proof/bind gates, limitations, defensive immutability and reflection/source exclusion of physical/layout/execution authority. The descriptor is data only. |
| Machine implementation catalog framework | IB-04 PASS | Three pure JVM catalog tests prove canonical identity ordering, filters, duplicate rejection and replacement; adapter/API tests prove immutable runtime attribution and mismatch rejection. The isolated v606 server publishes exactly `create:mechanical_millstone` and `create:mechanical_press`, proves logical item/rotational-power ports and C-03/C-04 evidence, follows reload fingerprints, mutates no world state and creates no session; `work/logs/create-runtime-recipe-catalog-20260717-003047.log`. |
| Binding typed-failure contract | IB-08 PASS | Exact 20-code enum and immutable complete-context tests cover stage, node/capability/recipe/candidates/Adapter/fingerprint/constraint/trace/intervention/next-step fields; candidate generation, independent verifier, runtime bridge and command propagate typed refusal rather than null/boolean/empty/exception conventions. |
| Implementation-bound graph and deterministic selection | IB-05/IB-06 PASS | Single/multi-step JVM tests prove original logical node/port/edge identity, recipe/quantity/Ingredient provenance, explicit score contributions, policy filtering, canonical tie-break, strict ambiguity and repeated trace stability. Reflection/source scans prove no layout, physical, execution, game or LLM authority. |
| Independent binding verification gate | IB-07/IB-08 PASS | Fifteen complete checks gate the non-publicly-constructible `VerifiedImplementationBoundPlan`; negative tests independently reject missing coverage/catalog identity, port mapping, quantities, Ingredient canonical identity, runtime/mod/Adapter/capability/recipe/evidence/policy and selection drift. The full 221-test run has zero failures; `work/logs/core-test-20260717-011156.log`. |
| Standard runtime implementation binding command | IB-09/IB-10/IB-12 PASS | Authoritative Create-only server binds gravel's two milling steps to the millstone and iron-sheet pressing to the press, retains exact/tag Ingredients, proves deterministic repeated plans, structured `/industrialagent bind create` success/failure/wrong-thread behavior, reload attribution and no mutation/session/crash; `work/logs/create-runtime-recipe-catalog-20260717-005237.log`. |
| DeceasedCraft implementation binding | IB-11 PASS | Isolated 288-mod server over 18,286 recipes binds four standard/custom scenarios, including KubeJS coke dust and two-step can pressing with aluminum tag identity; two typed stale/forbidden refusals and all fifteen checks pass, with zero world mutation/session/crash/residual process/external mutation/formal-save read; `work/logs/deceasedcraft-isolated-server-20260717-010552.log`. |
| Binding physical regression protection | IB-12 PASS | Clean 21-task build plus fresh C-02/C-03/C-04 isolated runs pass; final logs `work/logs/build-20260717-005725.log`, `create-kinetics-acceptance-20260717-010849.log`, `create-processing-gametest-20260717-010957.log`, `create-belt-press-gametest-20260717-011050.log` have zero strict failure markers. |
| Layout models, catalog and deterministic search | LP-01 through LP-05 PASS | Loader-neutral bounded footprint/clearance/component/port/snapshot/constraint values plus exact v606 geometry rules cover the two accepted implementations. JVM tests prove three orientations, single/multi-node placement and byte-for-value deterministic replay without a `(0,0,0)` placeholder, random order, LLM, session or executor. |
| Physical ports, ITEM/ROTATIONAL_POWER routing and capacities | LP-06 through LP-08 PASS | Orientation-dependent ITEM ports, deterministic bounded routes and descriptor-derived power topology are preserved in placements. Occupied/clearance/item-route/item-capacity/power/stress/stale/budget/unsupported cases return typed failures; no validation changes the world. |
| Unified graph and independent physicalization gate | LP-09/LP-10 PASS | The unchanged strict `UnifiedMachineGraph` receives process, boundary and power nodes with compatible directed ports/edges. Only all thirteen checks construct non-public `VerifiedPhysicalPlan`; exact fifteen layout codes and no session/handler/game/LLM authority are tested. |
| Standard runtime physicalization | LP-11 PASS | Actual Create-only world block states physicalize gravel and iron-sheet runtime bindings into six plans over three orientations, repeat identically, reject three capacity failures and refuse sequenced assembly; no world mutation/session/crash; `work/logs/create-runtime-recipe-catalog-20260717-014344.log`. |
| Pack runtime physicalization | LP-12 PASS | Actual isolated DeceasedCraft block states physicalize standard/KubeJS milling and pressing bindings into 12 plans with seven exact failures. Formal fingerprints remain unchanged, crash/residual processes are zero, and C-02/C-03/C-04 remain green; logs `deceasedcraft-isolated-server-20260717-015152`, `create-kinetics-acceptance-20260717-015429`, `create-processing-gametest-20260717-015551`, `create-belt-press-gametest-20260717-015658`. |
| Machine capability catalog | PASS | Four P-04 pure JVM tests cover immutable Adapter-owned capability/recipe IDs, typed input/output ports, bounded power/resource requirements, actions, collectible evidence, diagnostics and opaque version/fingerprint limits. Canonical recipe/Adapter/compatibility queries reject duplicate IDs and incompatible port or power declarations. Core contains no Create Java type and does not claim a real runtime machine mapping. |
| Process dependency graph | PASS | Ten P-05 pure JVM methods cover all twelve initial acceptance scenarios: milling and pressing single steps, a scaled two-step/multi-input chain, intermediate ITEM edges, raw leaves, optional byproduct accounting, stable alternatives, owned-intermediate bypass, quantity conversion, missing capability/recipe, cycle trace, depth limit, forbidden mod, material maximum and identical repeated results. Expansion is bounded and coordinate/world-free; it selects only supplied test-catalog facts. |
| Typed planning failure | PASS | Five P-06 pure JVM tests enforce exactly eleven stable codes, explicit resource/recipe location, nonempty dependency trace, bounded user-readable reason, canonical alternative IDs, defensive immutability, invalid-evidence rejection and explicit success/failure result branches. Planner context and scaling errors propagate as typed failures; no empty-list failure convention is permitted. |
| Coordinate-free candidate generation | PASS | Five P-07 pure JVM tests cover single/multi-step candidates, explicit selected recipes/capabilities/raw/intermediate/owned resources, processing order, scaled quantity conversions, dependency edges, evidence and estimated ticks. Each processing step has an immutable logical node with no position/orientation/implementation and an explicit `UnboundSpatialLayout`; SHA-256 candidate identity uses only logical dependency content and repeated alternatives are byte-for-byte equivalent as values. |
| Deterministic candidate scoring | PASS | Six P-08 pure JVM tests cover explicit lower-is-better weights and contribution breakdown for step count, machine kinds, raw kinds, unavailable capabilities, ordered mod preference, integer owned-utilization basis points and estimated/unknown time. P-02 strategy preferences apply only through an explicit bounded multiplier; BigInteger accumulation cannot overflow and equal totals use canonical candidate ID as the stable final tie-break. No LLM ranking is involved. |
| Logical machine graph mapping | PASS | Five graph-model and seven P-09 mapper JVM tests prove one coordinate-free PROCESS node per candidate step, exact raw/owned/intermediate/target resource identities and quantities, explicit aggregated capability power requirements, fully-owned targets, deterministic IDs/order and strict topology/ownership/mode/bounds. Candidate inconsistency, capability missing/incompatible, Adapter unavailable, unsupported resource type, unresolved input, target missing/ambiguous and quantity overflow are typed failures. No implementation, coordinate, orientation, side, physical port, handler or world reference exists; `UnifiedMachineGraph` remains the later unchanged physical target. |
| Read-only planning verification | PASS | Nine P-10 JVM tests run milling, pressing, fully-owned and two-step raw/owned/intermediate cases through the complete typed planning pipeline, require all eight immutable check records, and prove repeatability. Valid-model mutations for missing dependency/source, wrong quantity/target/capability/edge type, cycle and graph identity plus unavailable Adapter/unsupported type all return explicit typed failures without a plan. `VerifiedLogicalPlan` has no public constructor or execution/physical/world authority. Combined P-05 through P-10 tests cover all twelve initial acceptance scenarios without an LLM or Create executor. |
| Generic process specification | PASS | Six pure JVM tests cover immutable typed inputs/outputs/byproducts, recipe/capability/evidence requirements, collection/quantity/wait bounds and distinct C-03 millstone versus C-04 belt/press/chest contracts; fresh physical GameTests remain authoritative |
| Generic execution step | PASS | Six pure JVM tests cover sealed immutable steps, phase identity, typed conditions/actions, timeout/retry/cancel/rollback/evidence fields, bounds and defensive copies; descriptors remain non-executable until routed by the separately tested runner |
| Generic execution session | PASS | Eight session tests cover bounded immutable plans, current/completed steps, phase/attempt/ticks, full evidence, journal-linked world-change references, monotonic progress, allowlisted retry, explicit terminals and recovery timing rebasing that preserves cursor/evidence/journal identity. G-11 adds a separate serializable snapshot; live executable descriptors are still supplied only by a trusted runtime plan registry. |
| Bounded generic step runner | PASS | Thirteen focused runner tests cover normal evidence-collecting completion, precondition failure, timeout, cancellation before work and after one recorded world change, action failure, verification failure, cross-tick retry exhaustion, no action reinvocation while evidence is missing, rejection of failed evidence, registered-rule rejection, rule/step mismatch and no false timeout after an exact-rescan reload gap. Hard cap is one action invocation per tick. |
| World-change journal and conservative rollback | PASS | Five core journal tests plus one runner and one adapter cancellation test cover bounded immutable before/after block snapshots, optional schema-tagged block-entity data, session/step/tick source, modified-position listing, injected resources, irreversible processing, reverse-order exact-state rollback, conflicts, reports and typed no-compensation warnings. Each C-03/C-04 GameTest also cancels after one real placement, restores the exact prior state and proves later work stays stopped. Rollback is limited to 64 loaded exact-match block operations and is not persistence or complete resource rollback. |
| Recovery checkpoint and reconciliation | PASS | Core G-11 plus player SavedData tests cover canonical bounded session/plan/graph/journal round-trip, topology fingerprints, corrupt/unknown/trailing/oversized frames, trusted-definition drift, terminal/resource-history refusal, mandatory rescan, exact resume, timing rebase and typed stale results. Real C-03/C-04 two-JVM fixtures and the visible player BUILD reload complete without duplicate placement/input/output; uncertain PROCESS refuses and VERIFY is idempotent. Persisted descriptors never become execution authority. |
| Generic verification evidence | PASS | Exact eleven stable kinds plus immutable evidence ID/requirement/source step/source/target/schema-tagged observed and expected values/tick/pass/diagnostic fields; four model tests and runner integration prove failed evidence cannot satisfy completion |
| Generic verification rule | PASS | Eight rule tests cover required/optional success, complete-value retention, latest-observation replacement, typed rejection, pending/timeout boundary, adapter-specific permission, source/target/kind mismatch, bounds and refusal to omit any C-03/C-04 process evidence requirement. Both production plans register exact rules for all four steps. |
| Kinetic diagnostics | PASS | No-power, overload priority, healthy network and stress-disabled no-false-overload tests |
| Adapter API limits | PASS | Radius greater than 16 rejected |
| Adapter contracts | PASS | Create/Mekanism identity, optional-mod failure, immutable snapshot |
| Test-only third-party genericity proof | PASS | Three G-12 pure JVM tests use a virtual adapter that implements only `IndustrialModAdapter`: its three-node ITEM graph exposes explicit input/output ports and one processing capability; the unchanged generic plan/session/runner/rule either completes after one bounded handler invocation with input/process/output/custom evidence or fails terminally with a typed simulated jam and no evidence. The fixture is test source only and proves no real mod integration. |
| Automation safety gate | PASS | Unknown operations denied; nuclear category hard-disabled |
| Create registry scan | PASS | Fresh isolated Forge server observes real `create:shaft` ID/namespace/position/`axis=x`, command return/output, and unloaded-chunk preservation |
| Create kinetic telemetry | PASS | Fresh isolated Forge server observes stopped, same-axis positive/negative powered, and real overstressed networks through the strict 6.0.6 point adapter; wrong-thread/no-chunk-load rejection and canonical replay pass |
| Mekanism absent | PASS | `scripts/Test-MekanismAbsent.ps1`: real server-thread typed failure, clean shutdown, no new crash report |
| Create-only startup | SERVER PASS; CLIENT PASS | Both profiles initialize Create 6.0.6 and record typed Create success plus typed Mekanism absence; the client also reaches a rendered title screen and self-stops |
| Mekanism-only startup | BLOCKED | Client/server profiles require an official compatible Mekanism dependency pin before implementation or execution |
| Both installed | BLOCKED | Client/server dual-mod profiles require the same official Mekanism dependency pin before implementation or execution |
| Neither installed | SERVER PASS; CLIENT PASS; PACKAGED JAR SERVER PASS | Userdev `neither-server` and rendered-screen `neither-client` record two typed absences and stop. The final reobfuscated production JAR also passes as the only mod in an official Forge 47.4.10 server: both target adapters return `UNSUPPORTED_RUNTIME`, every dimension saves, the process exits inside 300 seconds, and strict class-loading/Mixin/thread/error/crash scans are empty. |
| Create real processing | C-03 GENERIC MILLSTONE PASS; C-04 GENERIC BELT/PRESS PASS | Separate structure-template GameTests prove real power, recipes, input and output at ZERO, CLOCKWISE_90 and CLOCKWISE_270. Both first prove G-14 infeasible-layout rejection with a complete conflict report and zero mutation, then emit exact generic graph/session/runner/rule/v606-handler markers, read back transformed physical block states, verify early-construction cancellation and safe exact-state rollback, and record completed journals with 10 block changes, one injected input and one irreversible process. C-04 additionally proves real belt transport, an observed 240-tick press cycle and chest output. |
| Mekanism real processing | NOT IMPLEMENTED | Energy increases/progress/output observed in GameTest |
| Save reload | G-11 GENERIC PASS; C-03 PRODUCTION PASS; C-04 PRODUCTION PASS | `scripts/Test-RecoveryReload.ps1` retains the minimal generic two-JVM proof. `scripts/Test-C03RecoveryReload.ps1` persists the real production session after all 10 BUILD changes but before input, narrowly canonicalizes only Create 6.0.6's transient `NeedsSpeedUpdate` byte on four live kinetic snapshots, requires exact equality for all remaining state, proves typed stale refusal, restores placement/step cursors and completes live milling with exactly one input/output. `scripts/Test-C04RecoveryReload.ps1` independently persists the 10-block-change BUILD boundary, performs the same narrow canonicalization on six live kinetic snapshots, proves typed stale refusal, restores build/step cursors and completes one real 240-tick pressing cycle with no duplicate placement/input/output. |
| Log/crash scan | PASS FOR M-01, T-02 USERDEV AND PACKAGED PROFILES, C-01, C-02, C-03, C-03 RELOAD, C-04, C-04 RELOAD, G-10, G-11, G-13, G-14, P-05, P-09 AND P-10 | No relevant `ERROR`/`FATAL`/Mixin/render/thread/class-loading/fixture-failure marker and zero new crash reports in each final accepted run; server rows also require all dimensions saved |

Passing a unit test never proves a real machine processed an item. End-to-end acceptance requires actual block entities, resources and outputs in isolated worlds.

Final stability-closure evidence on 2026-07-15: 115/115 pure JVM tests (`work/logs/core-test-20260715-164350.log`); 21/21 clean-build tasks with all tests, binary JARs and sources JARs (`work/logs/build-20260715-164425.log`); C-02 telemetry (`work/logs/create-kinetics-acceptance-20260715-164512.log`); three-orientation C-03 and C-04 physical processing (`work/logs/create-processing-gametest-20260715-164641.log`, `work/logs/create-belt-press-gametest-20260715-164750.log`); fresh two-JVM production reloads (`work/logs/c03-recovery-reload-acceptance-20260715-164858.log`, `work/logs/c04-recovery-reload-acceptance-20260715-165131.log`); and the exact packaged neither-mod JAR launch (`work/logs/packaged-neither-server-20260715-164140.log`). Strict scans of all eight accepted logs found no relevant ERROR/FATAL, Mixin, class-loading, thread, GameTest or fixture-failure marker, and the six current runtime directories contain zero crash reports. Mekanism-only and dual-mod rows remain `BLOCKED`, not inferred.

P-09 evidence on 2026-07-16: 166/166 pure JVM tests (`work/logs/core-test-20260716-174357.log`); 21/21 clean-build tasks with binary/sources JARs and artifact isolation (`work/logs/build-20260716-174431.log`); fresh C-02 (`work/logs/create-kinetics-acceptance-20260716-174524.log`), three-orientation C-03 (`work/logs/create-processing-gametest-20260716-174626.log`) and three-orientation C-04 (`work/logs/create-belt-press-gametest-20260716-174716.log`). The three runtime logs have zero relevant ERROR/FATAL, Mixin, class-loading, thread or GameTest-failure marker and each isolated run reports zero crash reports; pure-module import scanning remains clean.

Final P-10/Goal-Driven Planning Foundation evidence on 2026-07-16: 175/175 pure JVM tests (`work/logs/core-test-20260716-180653.log`); 21/21 clean-build tasks with binary/sources JARs and artifact isolation (`work/logs/build-20260716-180728.log`); fresh C-02 (`work/logs/create-kinetics-acceptance-20260716-180824.log`), three-orientation C-03 (`work/logs/create-processing-gametest-20260716-180941.log`) and three-orientation C-04 (`work/logs/create-belt-press-gametest-20260716-181042.log`). Strict scans found zero relevant ERROR/FATAL, Mixin, class-loading, thread or GameTest-failure marker, all current isolated runs have zero crash reports, pure modules have zero game/mod imports and P-10 has no execution/physical/world/LLM authority reference.

R-01 evidence on 2026-07-16: 179/179 pure JVM tests (`work/logs/core-test-20260716-190627.log`); 21/21 clean-build tasks with artifact isolation (`work/logs/build-20260716-190840.log`); and a fresh read-only server fixture (`work/logs/create-runtime-recipe-catalog-20260716-190526.log`) that observed 2,609 live recipes, explicit pre-R-02 typed mapping failure, reload-generation/fingerprint change, identical before/after probe state, all-dimension save, zero strict error markers and zero crash reports.

R-02 evidence on 2026-07-16: 185/185 pure JVM tests (`work/logs/core-test-20260716-193040.log`); 21/21 clean-build tasks with artifact isolation (`work/logs/build-20260716-193117.log`); and a fresh read-only server fixture (`work/logs/create-runtime-recipe-catalog-20260716-192900.log`). The actual manager contained 2,609 total recipes, 44 milling and 7 pressing; 25 milling plus all 7 pressing mapped, 19 milling entries emitted `OUTPUT_UNSUPPORTED` with full recipe trace, and 18 mapped secondary-output warnings remained visible. Required recipe IDs, quantities, `forge:ingots/iron`, source/fingerprint attribution, reload rebuild, no mutation, all-dimension save, strict error scan and zero crash reports passed.

R-03 evidence on 2026-07-16: 193/193 pure JVM tests (`work/logs/core-test-20260716-194116.log`) and 21/21 clean-build tasks (`work/logs/build-20260716-194154.log`). Focused tests cover all four ingredient kinds, canonical CRC-protected round-trip/rejection, owned-resource preference, deterministic ties/fallback, forbidden candidates, original recipe/tag/ingredient evidence, partial-catalog limitations, all-unresolved failure and stale tag fingerprints. Mandatory physical protection passed for C-02 (`work/logs/create-kinetics-acceptance-20260716-194247.log`), C-03 (`work/logs/create-processing-gametest-20260716-194424.log`) and C-04 (`work/logs/create-belt-press-gametest-20260716-194531.log`) with zero strict errors/crash reports.

R-04 evidence on 2026-07-16: 195/195 pure JVM tests (`work/logs/core-test-20260716-195928.log`) and 21/21 clean-build tasks (`work/logs/build-20260716-200043.log`). The real-server fixture (`work/logs/create-runtime-recipe-catalog-20260716-195755.log`) proves exactly two runtime-attributed capabilities, matching recipe types, ITEM ports, continuous ROTATIONAL_POWER, typed evidence/diagnostics, accepted v606 fingerprint, physical proof limited to the C-03/C-04 recipe IDs, reload fingerprint replacement and no world mutation. Mandatory physical protection passed for C-02 (`work/logs/create-kinetics-acceptance-20260716-200141.log`), C-03 (`work/logs/create-processing-gametest-20260716-200256.log`) and C-04 (`work/logs/create-belt-press-gametest-20260716-200356.log`) with zero strict errors/crash reports.

R-05 evidence on 2026-07-16: 199/199 pure JVM tests (`work/logs/core-test-20260716-201759.log`) and 21/21 clean-build tasks (`work/logs/build-20260716-201832.log`). Four focused tests cover upward batch rounding, owned tag resolution, equal-score repeatability, missing recipe/capability, snapshot mismatch and absence of physical authority types. The final real-server fixture (`work/logs/create-runtime-recipe-catalog-20260716-201631.log`) selected one deterministic two-step andesite→cobblestone→gravel plan for quantity 3 and one deterministic two-batch `create:pressing/iron_ingot` plan, retained real IDs/tag/fingerprint, passed all eight verification checks, made no world change and ended with zero crash reports. Mandatory physical protection passed for C-02 (`work/logs/create-kinetics-acceptance-20260716-201926.log`), C-03 (`work/logs/create-processing-gametest-20260716-202041.log`) and C-04 (`work/logs/create-belt-press-gametest-20260716-202136.log`) with zero strict errors/crash reports.

R-06 evidence on 2026-07-16: 199/199 pure JVM tests including formatter success/failure field coverage (`work/logs/core-test-20260716-203250.log`) and 21/21 clean-build tasks (`work/logs/build-20260716-203327.log`). The final isolated server (`work/logs/create-runtime-recipe-catalog-20260716-203138.log`) executed `/industrialagent plan create minecraft:gravel 3`, returned one with complete concise/structured verified evidence, returned zero with typed `RECIPE_NOT_FOUND` for diamond, parser-rejected zero quantity and malformed ResourceId, made no world change, saved all dimensions and had zero strict errors/crash reports.

R-07 evidence on 2026-07-16: 199/199 pure JVM tests (`work/logs/core-test-20260716-204650.log`) and 21/21 clean-build tasks (`work/logs/build-20260716-204737.log`). The final Create-only server (`work/logs/create-runtime-recipe-catalog-20260716-204542.log`) accounted for 2,609 total recipes, all 44 milling and 7 pressing entries, 32 successful mappings, 19 typed `OUTPUT_UNSUPPORTED` limitations and 18 warnings; real gravel/iron-sheet plans, multi-quantity conversion, deterministic repetition, exact/tag identity, command success/failure/parser/off-thread behavior, reload replacement, no world/session mutation, authority scan, all-dimension save and zero strict errors/crashes passed. The packaged neither-mod server (`work/logs/runtime-neither-server-20260716-204421.log`) returned `REQUIRED_MOD_UNAVAILABLE` for the same command and had zero class-loading errors or crash reports. Focused JVM tests cover non-divisible upward rounding, `RECIPE_NOT_FOUND`, `MACHINE_CAPABILITY_MISSING`, ingredient choice and complex-input refusal, snapshot mismatch and deterministic ties.

R-08 evidence on 2026-07-16: the repeatable audit (`scripts/Audit-DeceasedCraftRuntimeKnowledge.ps1`, `work/logs/deceasedcraft-runtime-readonly-audit-20260716-205323.log`) read only scoped mod/config/default-config/KubeJS/generated-datapack files, excluded saves/worlds, did not launch either formal instance and observed the same metadata fingerprint before and after. It records the exact 1.20.1/Forge 47.4.0/Create 6.0.6 inputs, 235 server mods, 10 Create-related server JARs, matching server scripts, one direct custom milling, one direct custom pressing and 30 sequenced pressing steps. Therefore formal-pack runtime recipe counts remain unverified. The 199/199 JVM suite (`work/logs/core-test-20260716-205339.log`) and 21/21 clean build (`work/logs/build-20260716-205418.log`) remain green.

R-09A evidence on 2026-07-16: `scripts/Test-DeceasedCraftProfileBuilder.ps1` proves safe ignored path acceptance, no-write plan mode, explicit forbidden scopes, and typed rejection of repository-external and `D:\PCL2` game directories. `scripts/New-DeceasedCraftIsolatedProfile.ps1 -Refresh` copied and triple-hash-verified 6,456 approved files into separate retained-source and writable-run trees, wrote a 236-JAR metadata inventory, skipped five nested options/server-list files, and produced equal formal client/server fingerprints before and after (`work/logs/deceasedcraft-profile-builder-20260716-213258.log`). All 199 JVM tests remain green (`work/logs/core-test-20260716-213756.log`). No service process was launched and no RecipeManager count is claimed yet.

R-09B evidence on 2026-07-16: the 21-task clean build passed all 199 JVM tests and artifact isolation (`work/logs/build-20260716-214434.log`). `scripts/Test-DeceasedCraftIsolatedServer.ps1 -SkipBuild` installed only that build's SHA-256 `f95777643f94ee0cd2d747d66ea5a9511bcd697917b6c7cd1678da62d3a6536c` production JAR into the run copy, printed and revalidated the exact ignored gameDir, then loaded Minecraft 1.20.1/Forge 47.4.0/Create 6.0.6 and 288 mod containers with no filtering. The production probe read back the world under `run/r09-world`, reported no session/world mutation authority, saved all dimensions and exited inside its bounds with zero residual process/crash report. Formal source fingerprints matched before and after (`work/logs/deceasedcraft-isolated-server-20260716-215412.log`). The log also retains 42 nonfatal optional-class/DISTXFORM/thread diagnostic matches; they are documented, not hidden or treated as a clean-log claim.

R-09C evidence on 2026-07-16: final clean build `work/logs/build-20260716-221852.log` passed all 21 tasks/199 JVM tests after confining the Create-specific exporter to `adapter/create/internal/v606`. The repeat isolated production run (`work/logs/deceasedcraft-isolated-server-20260716-221951.log`) exported 18,286 real manager recipes with all type counts summing exactly to that total. Create counts are milling 66, pressing 37, sequenced assembly 91, mixing 67, compacting 26, crushing 103, cutting 488, deploying 113 and splashing 39. Existing R-02 mapping accounted for every milling/pressing entry as 45/37 mapped or 21 `OUTPUT_UNSUPPORTED`, with 31 probabilistic-byproduct and four guaranteed-secondary warnings. The runtime exposed 59 `create:kjs/*` recipes; custom coal-coke milling preserves exact input/output and custom aluminum pressing preserves the `forge:plates/aluminum` tag and resolved candidate. Ninety-one sequences retain 309 ordered steps, 38 pressing steps, loop count and transitional item while returning typed unsupported. The run saved/exited with no crash/process/external modification and did not create a session or mutate the world through the probe.

R-09D evidence on 2026-07-16: the standard isolated RecipeManager rerun exported 2,609 recipes and retained the accepted 44 milling/7 pressing/32 mapped/19 rejected boundary (`work/logs/create-runtime-recipe-catalog-20260716-222711.log`). `scripts/Compare-DeceasedCraftRuntimeKnowledge.ps1` validates authoritative schemas and type-count conservation, compares normalized Ingredient identity/candidates, output/byproduct, type, mapping status and duration, and writes ignored `runtime-difference.json`. Its two-sided accounting proves all 51 standard rows are unchanged and the pack adds 52 rows with no removal or modification; the two mapped `create:kjs/*` rows retain exact/tag semantics. Fingerprints differ as required and static script counts are not accepted as runtime evidence.

R-09E evidence on 2026-07-16: the clean build passed 21 tasks/199 tests (`work/logs/build-20260716-224256.log`), then the isolated production server completed in 193.2 seconds (`work/logs/deceasedcraft-isolated-server-20260716-224346.log`). Four runtime-backed goals (gravel, iron sheet, KubeJS coke dust, KubeJS can) each carry exactly eight verification checks and output `VerifiedLogicalPlan`; quantities are greater than one, both standard and custom tag selections are retained, and repeated coke-dust planning is identical. Missing target is `RECIPE_NOT_FOUND`, `create:milling/bone` remains catalog `OUTPUT_UNSUPPORTED` and is not planned, and disabled capability discovery is `CAPABILITY_CATALOG_MISSING`. The JSON authority scan found no coordinates/orientation/implementation/physical graph/session fields. The run left zero process/crash, retained the known 42 diagnostics and proved both formal fingerprints unchanged.

R-09F final evidence on 2026-07-16: `scripts/Test-DeceasedCraftR09.ps1` passed profile safety and refreshed 6,456 files/236 JARs, then 199 JVM tests (`work/logs/core-test-20260716-225732.log`), all 21 clean-build tasks (`work/logs/build-20260716-225837.log`), C-02 (`work/logs/create-kinetics-acceptance-20260716-225959.log`), three-orientation C-03 (`work/logs/create-processing-gametest-20260716-230129.log`), three-orientation C-04 (`work/logs/create-belt-press-gametest-20260716-230220.log`), standard runtime (`work/logs/create-runtime-recipe-catalog-20260716-230308.log`) and pack runtime/planning (`work/logs/deceasedcraft-isolated-server-20260716-230411.log`). Final comparison remains 51 unchanged plus 52 added. Independent scans found zero strict standard/fatal pack markers, crash reports and residual processes, while retaining 42 documented nonfatal pack diagnostics. Final external fingerprints match their pre-run values and no save/formal world was read.

Final Create Runtime Knowledge Foundation evidence on 2026-07-16: 199/199 JVM tests (`work/logs/core-test-20260716-205917.log`), all 21 clean-build tasks (`work/logs/build-20260716-205951.log`), C-02 four-state telemetry/thread/chunk/replay acceptance (`work/logs/create-kinetics-acceptance-20260716-210041.log`), three-orientation C-03 real milling (`work/logs/create-processing-gametest-20260716-210142.log`), three-orientation C-04 real belt/240-tick press/chest production (`work/logs/create-belt-press-gametest-20260716-210240.log`) and final runtime recipe/planning/reload/command acceptance (`work/logs/create-runtime-recipe-catalog-20260716-210338.log`) all pass. A combined strict scan of those logs found zero relevant ERROR/FATAL, exception, Mixin, class-loading, thread or GameTest-failure markers, and all accepted run directories contain zero crash reports.

## Repeatable C-01 registry acceptance

Run the completed C-01 acceptance with:

```powershell
.\scripts\Test-CreateScan.ps1
```

The driver deletes only the ignored repository directory `forge-create-1.20.1/run/create-scan-acceptance/`, starts a new non-public Forge 47.4.10 dedicated-server world with Create 6.0.6-150, and runs an automated server-thread fixture equivalent to the required bounded GameTest. It checks:

- a real block obtained from the active Forge registry is placed as `create:shaft` with `axis=x`;
- the production adapter returns one component with the exact namespace, block ID, absolute position, state and radius 1;
- the registered `/industrialagent scan create 1` path returns 1 and emits the expected Create summary;
- a remote chunk is unloaded before capture, produces typed `CHUNK_NOT_LOADED`, and remains unloaded afterward;
- the server saves every dimension, emits no `ERROR`, `FATAL`, thread exception or fixture-failure marker, and creates no crash report.

Evidence: `work/logs/create-scan-acceptance-20260713-121850.log`.

## Repeatable C-02 kinetic acceptance

Run the completed C-02 acceptance with:

```powershell
.\scripts\Test-CreateKinetics.ps1
```

The driver deletes only the ignored repository directory `forge-create-1.20.1/run/create-kinetics-acceptance/`, creates a fresh deterministic world, and starts a non-public Forge 47.4.10 dedicated server with Create 6.0.6-150. It is an automated tick-driven server fixture equivalent to an isolated GameTest and allows Create to settle naturally without sleeping or blocking the server thread. The fixture-only `create-server.toml` keeps stress enabled, retains the real fan impact `2 SU/RPM`, and lowers only the creative motor capacity to `1 SU/RPM`; production defaults are unchanged.

The fixture proves:

- an isolated real `create:shaft` is stopped with no fabricated network or stress totals;
- two real creative motors sharing `axis=x` but facing opposite directions report actual `+16 RPM` and `-16 RPM`, proving direction comes from Create's signed speed rather than registry axis;
- a real motor/fan network settles to capacity `16 SU`, load `32 SU`, actual speed `0`, `STATIONARY`, and `overstressed=true` through Create's own stress propagation;
- a named worker receives typed `WRONG_THREAD` before world access, without any server-thread wait;
- an unloaded remote chunk returns typed `CHUNK_NOT_LOADED` and stays unloaded;
- all four immutable live snapshots round-trip through the canonical replay codec, while the pure JVM golden fixture fixes the byte encoding and replays stopped/powered/overstressed semantics;
- every dimension saves, no relevant fatal/error marker or crash report appears, and the server stops cleanly.

Evidence: `work/logs/create-kinetics-acceptance-20260713-125833.log`.

## Repeatable C-03 processing acceptance

Run the completed C-03 acceptance with:

```powershell
.\scripts\Test-CreateProcessing.ps1
```

The driver deletes only the ignored repository directory `forge-create-1.20.1/run/create-processing-gametest/`, writes a deterministic classic-flat non-public server configuration, and starts Forge's auto-closing GameTest server. Forge 47.4.10 successfully registers three orientation tests against deterministic empty structure template `minecraft:bastion/mobs/empty`; no dedicated-server fallback is used.

The GameTest proves:

- a loader-neutral plan fixes 10 ordered placements and a 175-position preflight volume capped at 256;
- before any executor/session exists, the final-role placement gate reports every one of 10 targets for a remote unloaded plan without loading its chunk; an obstacle/protection fixture then reports four typed conflicts at all three affected positions and proves rejected `begin` leaves all ten target states unchanged;
- the compatibility `WaterWheelMillstonePlan` is converted to a six-node relative graph and four generic steps; the shared session/runner/rules dispatch only the exact v606 handler, which places and reads back the water catch, real `create:water_wheel`, `create:gearbox`, vertical `create:shaft`, `create:millstone` and water source at the planned coordinates and axes;
- an independent session performs its first real placement, records the complete before/after snapshot, then cancellation restores the exact prior block and a later tick cannot invoke another action;
- the completed physical run records exactly 12 journal entries: 10 block changes, one injected cobblestone and one irreversible milling result; those resource records are warnings rather than permission to synthesize compensation;
- live Create block entities report `8 RPM` at the water wheel, gearbox, shaft and millstone;
- the active recipe manager resolves `create:milling/cobblestone` as `create:milling` with duration `250` and a deterministic gravel result;
- one cobblestone is inserted into the real millstone input inventory, is consumed by Create processing, and one gravel is observed in its real output inventory;
- a named worker receives typed `WRONG_THREAD`, while a remote bounded plan receives `CHUNK_NOT_LOADED` and leaves the chunk unloaded;
- the default typed anchor remains backward-compatible, while CLOCKWISE_90 and CLOCKWISE_270 rotate all coordinates and physical water-wheel/gearbox/shaft axes together; their readback markers prove the graph/template was not rotated only in memory;
- GameTest reports 3/3 required tests passed, all dimensions save, no forbidden error/thread marker or crash report appears, and the server exits automatically.

Latest G-14 evidence retaining the G-13 transforms, completed-G-10 journal/cancellation and generic path: `work/logs/create-processing-gametest-20260715-121744.log`.

## Repeatable C-04 belt/press acceptance

Run the completed C-04 acceptance with:

```powershell
.\scripts\Test-CreateBeltPress.ps1
```

The driver deletes only the ignored repository directory `forge-create-1.20.1/run/create-belt-press-gametest/`, creates a deterministic classic-flat non-public configuration with Forge's unrelated online version check disabled, and starts three auto-closing Forge GameTests against `minecraft:bastion/mobs/empty`.

The GameTest proves:

- the loader-neutral plan fixes eight ordered build steps, eight final roles and a 6 x 5 x 5 (150-position) loaded-area preflight capped at 256;
- before any executor/session exists, the final-role placement gate reports every one of 8 targets for a remote unloaded plan without loading its chunk; an obstacle/protection fixture then reports four typed conflicts at all three affected positions and proves rejected `begin` leaves all eight target states unchanged;
- the compatibility `BeltPressPlan` is converted to a nine-node relative graph and four generic steps; the shared session/runner/rules dispatch only the exact v606 handler, which places two temporary pulley shafts, two real creative motors, a real mechanical press, chest and insertion-mode andesite funnel, then uses Create's belt connector operation to create and read back a real three-segment horizontal belt;
- an independent session performs its first real pulley-shaft placement, journals its block-entity-capable snapshot, then cancellation restores the exact prior block and prevents any later construction;
- the completed physical run records exactly 12 journal entries: 10 block changes (including all three belt-connection mutations), one injected iron ingot and one irreversible pressing result; it never generates compensating items;
- live block entities report `16 RPM` at the belt motor, three belt segments, press motor and mechanical press, with no overstress or unsettled network state;
- the active recipe manager resolves `create:pressing/iron_ingot` as `create:pressing`; one iron ingot enters the real belt item handler, the press exposes a real `BELT` cycle with `240` cycle ticks, and one iron sheet reaches the real chest through the funnel;
- completion evidence requires all final positions, six positive kinetic readings, the exact live recipe/input/output, observed belt input, an observed press cycle and observed chest output;
- a named worker receives typed `WRONG_THREAD`, while a remote bounded plan receives `CHUNK_NOT_LOADED` and leaves its chunk unloaded;
- CLOCKWISE_90 and CLOCKWISE_270 use typed anchors contained inside their isolated templates and physically read back transformed belt axis/movement facing, both drive facings, mechanical-press facing and upward funnel facing before accepting output;
- GameTest reports 3/3 required tests passed, every dimension saves, no forbidden error/thread marker or crash report appears, and the server exits automatically.

Latest G-14 evidence retaining the G-13 transforms, completed-G-10 journal/cancellation and generic path: `work/logs/create-belt-press-gametest-20260715-121653.log`.

## Repeatable G-11 recovery reload acceptance

Run the bounded two-process fixture with:

```powershell
.\scripts\Test-RecoveryReload.ps1
```

The driver recreates only `forge-create-1.20.1/run/recovery-reload-acceptance/`. Its first Forge server process completes one generic step, performs and journals one real vanilla stone placement during the next action, pauses that action, encodes the complete session and journal, stores the checkpoint through Minecraft `SavedData`, saves every dimension and exits. A second independent JVM opens the same world and proves:

- the old session ID, current step, completed-step prefix and modified position survived the process boundary;
- discovery exposes only `RescanRequired`, not an executable session, and matches the persisted graph/plan fingerprints against an explicitly registered trusted runtime plan;
- `ForgeRecoveryWorldScanner` runs on the authoritative server thread, does not load chunks and captures the exact registered block/properties/optional full block-entity SNBT;
- an exact after-state rescan permits one bounded resume handler invocation without a world mutation;
- changing the recorded stone to dirt makes the same candidate return `steve_industrial:recovery/stale_session` with reason `WORLD_STATE_CHANGED`, and reconciliation does not repair or otherwise mutate that fault;
- both server processes save every dimension, both Gradle runs succeed, the strict error/thread/Mixin/crash scan is empty, and the isolated run creates no crash report.

Final evidence: `work/logs/recovery-reload-acceptance-20260715-105505.log`. This fixture uses a generic two-step plan and vanilla block; it does not itself exercise a production Create handler. C-03 and C-04 have separate bounded rows below. Injected resources and completed processing are deliberately not resumed.

## Repeatable production C-03 recovery reload acceptance

Run the bounded two-process production fixture with:

```powershell
.\scripts\Test-C03RecoveryReload.ps1
```

The driver recreates only `forge-create-1.20.1/run/c03-recovery-reload-acceptance/`, computes a clear anchor above the complete transformed preflight footprint without modifying terrain, and enforces a 420-second wall-clock bound on each Forge server process. The write process uses the real `ForgeCreatePlanAdapter` and v606 C-03 handler, completes all ten BUILD placements, then persists only the safe `POWER/READY` cursor with ten `BlockChange` entries and no resource history. The read process proves:

- the trusted generic plan fingerprint, physical anchor, completed BUILD prefix, step state and placement cursor survive the JVM/world reload;
- the raw post-load scan fails exact comparison on Create 6.0.6's transient kinetic byte, while the versioned adapter removes only that byte from exactly four live `KineticBlockEntity` snapshots after validating the exact trusted position set;
- the canonical authoritative rescan is mandatory before the production resume entry is available;
- changing one recorded stone to dirt yields typed `steve_industrial:recovery/stale_session` / `WORLD_STATE_CHANGED` and does not mutate the fault;
- restoring the fixture state permits resume at placement cursor 10, and no later progress reports BUILD or a lower placement count;
- live Create 6.0.6 power and `create:milling/cobblestone` are used after recovery, exactly one cobblestone is consumed, exactly one gravel is observed, and the final journal contains 10 block changes, one injected input and one irreversible process;
- both JVMs save every dimension and exit automatically, strict ERROR/FATAL/Mixin/class-loading/thread scans are empty, and the fresh isolated run contains no crash report.

Final evidence: `work/logs/c03-recovery-reload-acceptance-20260715-164858.log`. This is not a claim that checkpoints after input injection can resume: G-11 still returns typed `RESOURCE_HISTORY_UNSAFE` for resource-bearing history.

## Repeatable production C-04 recovery reload acceptance

Run the bounded two-process production fixture with:

```powershell
.\scripts\Test-C04RecoveryReload.ps1
```

The driver recreates only `forge-create-1.20.1/run/c04-recovery-reload-acceptance/` and enforces a 420-second bound per JVM. The write process runs the real C-04 adapter/handler through all eight BUILD actions, settles the live layout, and persists only `POWER/READY` with 10 block changes across eight final positions and no resource history. The read process proves:

- raw Create 6.0.6 post-load kinetic initialization state is not accepted as exact; the v606 adapter then removes only the ignored transient byte `NeedsSpeedUpdate` from exactly six live `KineticBlockEntity` snapshots, while every block identity/property and all remaining block-entity SNBT stay exact;
- changing the planned output chest to dirt returns typed `steve_industrial:recovery/stale_session` / `WORLD_STATE_CHANGED` without reconciliation mutation;
- exact restored state permits resume with build cursor 8 and the recovered generic step cursor, without re-entering BUILD or repeating input;
- live `create:pressing/iron_ingot` consumes exactly one iron ingot, exposes the real 240-tick press cycle and stores exactly one iron sheet in the planned chest;
- the final journal has 10 block changes, one injected input and one irreversible process, with no duplicate placement, input or output;
- both JVMs save and exit, the strict ERROR/FATAL/Mixin/class-loading/thread scan is empty and the fresh run has zero crash reports.

Final evidence: `work/logs/c04-recovery-reload-acceptance-20260715-165131.log`. This does not allow resource-bearing checkpoints to resume and does not relax generic exact reconciliation for other adapter data.

## Repeatable T-02 server profiles

Run both currently implemented server profiles with:

```powershell
.\scripts\Test-RuntimeMatrix.ps1
```

Run one profile with `scripts/Test-RuntimeProfile.ps1 -Profile neither-server` or `-Profile create-only-server`. The driver deletes only the selected ignored directory under `forge-create-1.20.1/run/profiles/`, recreates a minimal non-public dedicated-server configuration, and then checks:

- Java 17, Minecraft 1.20.1 and Forge 47.4.10 launch evidence;
- exact per-mod typed results and a stable `RUNTIME_PROFILE_SMOKE PASS` marker;
- Create 6.0.6 initialization only in `create-only-server`;
- complete world save, no `ERROR`/`FATAL`/thread exception/profile failure, and no new crash report.

The Gradle property `industrialRuntimeProfile` controls both the isolated working directory and the userdev dependency set. These profiles do not access or mutate the external launcher instance or its saves.

## Repeatable T-02 packaged production-JAR server

Run the completed packaged neither-mod row with:

```powershell
.\scripts\Test-PackagedNeitherServer.ps1
```

The driver first runs the repository clean build and selects exactly one reobfuscated non-sources JAR. It verifies that the JAR contains core, adapter API, Forge host and Create-adapter entries, declares Create and Mekanism optional, embeds neither target mod's classes and excludes the userdev-only JourneyMap lookup target. It then:

- downloads the pinned official Forge 47.4.10 installer only when the ignored cache is absent and requires SHA-1 `66BFEA9963BFA60D88BAB6B2750E74A958392715`;
- recreates only `forge-create-1.20.1/run/packaged-neither-server/` and installs the SHA-256-identical production JAR as its sole mod;
- starts the real `forgeserver` launch target with a 300-second wall-clock bound and no Create or Mekanism JAR;
- requires typed `UNSUPPORTED_RUNTIME` results for both optional mods, the stable profile PASS marker, all-dimension save, process exit 0 and zero crash reports;
- rejects relevant `ERROR`/`FATAL`, Mixin failure, thread exception, `ClassNotFoundException`, `NoClassDefFoundError`, crash preparation or mod-loading exception markers.

Final evidence: `work/logs/packaged-neither-server-20260715-164140.log`; its input clean build is `work/logs/build-20260715-164053.log`, and the packaged JAR SHA-256 is `73CC6FBF2FED8A98DF422D3E06BF12A97E78A24BD2CFE2D2F33D3F99D3364EEA`.

## Repeatable T-02 userdev client profiles

Run both completed client rows with:

```powershell
.\scripts\Test-ClientRuntimeMatrix.ps1
```

Run one row with `scripts/Test-ClientRuntimeProfile.ps1 -Profile neither-client` or `-Profile create-only-client`. The driver recreates only that ignored directory under `forge-create-1.20.1/run/profiles/`, starts the real Forge userdev client and enforces a 300-second wall-clock limit. Acceptance requires:

- Java 17, Minecraft 1.20.1 and Forge 47.4.10 launch evidence;
- Steve Industrial Agent's loaded mod-container version;
- a real `ScreenEvent.Render.Post` for the rendered title screen on the client thread;
- exact typed results from `ForgeRuntimeAvailabilityAdapter`: two `UNSUPPORTED_RUNTIME` failures for neither-client, or Create 6.0.6-150 success plus Mekanism `UNSUPPORTED_RUNTIME` for create-only-client;
- the stable profile PASS marker, explicit verified exit request, Minecraft `Stopping!`, successful Gradle exit, no forbidden ERROR/FATAL/ClassNotFound/Mixin/render/thread marker and no crash report.

Create 6.0.6 unconditionally asks Mixin to resolve its optional JourneyMap target during startup. The create-only profile supplies only that empty target name through a run-only source set while asserting the actual JourneyMap mod is absent; the class is never loaded. `scripts/Build.ps1` rejects either production JAR if this target leaks into it.

Final matrix evidence: `work/logs/runtime-neither-client-20260714-131558.log` (36 seconds) and `work/logs/runtime-create-only-client-20260714-131634.log` (43.9 seconds). These prove userdev client startup only; the packaged server row is separate, and Mekanism-bearing rows remain blocked.

## Goal-driven execution integration

Run the execution acceptance with:

```powershell
.\scripts\Test-GoalDrivenExecution.ps1
```

The integrated isolated Forge/Create server now requires all 42 registered
tests. They retain the original three-orientation gravel x3, iron sheet x2,
typed refusal, cancellation/duplicate-session, reload/recovery, route, power
and output checks and add C-03 through C-10 Direct/Bots/Hybrid, fault,
cancellation and exact-recovery coverage. Success requires all
planning/binding/physicalization/
readiness check counts, real recipe and quantity evidence, input consumption,
output observation, bounded journals/traces and a maximum of one handler call
per server tick. Current integrated evidence is
`work/logs/goal-driven-execution-gametest-20260727-191342.log`; all 42 tests
passed with no duplicate placement/input/output, crash report or residual
process.

## Phase III dual-role Bot presentation

| Scope | Evidence | Result |
|---|---|---|
| Distinct useful roles | Steve logistics starts one material assignment; Alex builder/inspector starts two build/verify assignments | PASS, C-03 and C-04 Bots GameTests |
| Smooth physical movement | Both roles record positive bounded movement ticks; no teleport fallback | PASS |
| Non-overlapping completion | Builder/inspector selects a safe verifier station excluding the logistics delivery station; completion rejects duplicate final positions | PASS, `completionStationsDistinct=true botOverlap=false` |
| Three-mode equivalence | Same verified physical plan/graph retains final snapshot, orientation, ports, output, evidence, cleanup, reload and materials across Direct/Bots/Hybrid | PASS, 18/18 `scripts/Test-GoalDrivenExecution.ps1` |
| Integrated regression | Core 416, Adapter 48, Forge 12; zero failures/errors/skips; artifact isolation | PASS, 476/476 and 23/23 build tasks |
| Player-visible final spacing | Committed JAR produced iron sheet 2/2; Steve ended at `-846,149,3001`, Alex at `-845,149,3002`; user confirmed separation | PASS |

Evidence: `work/logs/goal-driven-execution-gametest-20260723-184939.log`,
`work/logs/core-test-20260723-185238.log`, and
`work/logs/build-20260723-185701.log`. Player-visible evidence:
`work/logs/iwp-visible-bot-spacing-20260723-191220.log`; journal cleanup
returned the region to the 256-block baseline with zero BlockEntities,
containers, dangerous blocks or unknown removals, and the client saved all
dimensions with zero crash reports/residual processes.

## Controlled production pilot

Run the production-JAR DeceasedCraft pilot only against the disposable marked profile:

```powershell
.\scripts\Test-DeceasedCraftIsolatedServer.ps1 -ExecutionPilot -SkipBuild -StartupTimeoutSeconds 240
```

Acceptance requires the normal R-09 knowledge/planning/binding/physicalization matrix plus two actual execution goals: `immersiveengineering:dust_coke x4` and tag-backed `apocalypsenow:can x3`. Both must use live RecipeManager IDs, retain the runtime fingerprint and Ingredient identity, pass 8/15/13/16 verifier checks, construct only the verified layout/routes, consume real chest-backed inputs, produce exact output quantities through Create, record every journal/phase/trace, invoke at most one handler per server tick, clean the controlled area, save/exit, leave no Java process/crash report and preserve both formal external fingerprints.

Final pack evidence is `work/logs/deceasedcraft-isolated-server-20260717-054415.log` plus `work/isolated-pack/deceasedcraft-r09/run/r09-evidence/execution-pilot.json`. Final standard and regression evidence is `goal-driven-execution-gametest-20260717-053827.log`, `core-test-20260717-054338.log`, `build-20260717-053735.log`, and C-02/C-03/C-04 logs `20260717-054004`, `054142`, `054245`.

## IWP-01 through IWP-11 accepted isolated-player pilot

| Scope | Evidence | Result |
|---|---|---|
| Test/formal discrimination | Unique marker/path/instance/fingerprint success; formal identity, missing marker and multiple-world refusal | PASS, six-method `WritableTestWorldPilotTest` |
| Region here and pos1/pos2 | Exact bounded geometry, corner-order normalization, 64x64x32 cap and build-height refusal; live two-node gravel correctly refused an undersized region | PASS |
| Preview/confirm binding | Preview required; world/dimension/player/session/expiry/fingerprint drift typed; no mutation authority | PASS |
| Fault: unconfirmed region | `REGION_NOT_SELECTED`/unconfirmed context refuses before readiness/start | PASS, `WritableTestWorldPilotTest` plus live post-cleanup refusal |
| Fault: stale region hash | `REGION_HASH_STALE` and changed world fingerprint refuse before mutation or resume | PASS, pure/recovery tests |
| Fault: wrong world/dimension | Exact marked-world identity and dimension are mandatory; mismatches fail typed | PASS, pilot identity/region/recovery tests |
| Fault: test world not backed up | Missing/invalid accepted IWP-05 evidence blocks readiness and first write | PASS, backup/readiness verifier tests |
| Fault: obstacle block | Protected/non-replaceable occupied cells fail layout/readiness with no partial placement | PASS, physicalization and pilot failure vocabulary tests |
| Fault: important BlockEntity | Important BlockEntity/container evidence blocks mutation and cleanup | PASS, preview/risk/readiness/cleanup tests |
| Fault: duplicate session | Second active session is rejected and the first remains authoritative | PASS, goal GameTest `duplicateRejected=true` |
| Fault: cancel | Cancel stops later phases and preserves a cleanup-owned journal | PASS, GameTest plus visible gravel BUILD cancel |
| Fault: cleanup ownership | Only exact journal-owned current states may be removed; drift/unknown positions refuse | PASS, strict Forge tests plus live unknown removals 0 |
| Fault: TEST_ONLY crossing | TEST_ONLY approval/material/power cannot authorize a formal-world operation | PASS, approval/readiness/formal tests |
| Fault: formal hard stop | Formal execution remains impossible before and after session construction | PASS, goal handler guard and formal write-guard tests |
| Full pure regression | `scripts/Test-Core.ps1` | PASS, `core-test-20260720-165646.log` |
| Clean build | 23 tasks; 402 aggregate JVM tests; production reobfuscated JAR | PASS, `build-20260720-170159.log` |
| Independent instance | 6,302 files/815,630,911 bytes hash-verified; separate saves/config/logs; exact JAR deployed | PASS, `iwp-instance-builder-20260720-171447.log` |
| Client launch plan | Java 17, 94 libraries, 71 arguments, fixed offline identity, no account/token read | PASS (`Start-IsolatedWritablePlayerInstance.ps1 -PlanOnly`) |
| Windows PowerShell launcher | 5.1 command-line construction with spaced paths; inherited base client `1.20.1.jar` is in Forge's ignore list; plan creates no process | PASS (`Test-IsolatedWritablePlayerInstanceLauncher.ps1`) |
| Live player-world identity | One closed `Steve Agent Test`; exact marker; `level.dat` SHA-256 `c51a54b...1fd17`; no matching process | PASS, IWP-01 |
| Live readiness/dry-run | Gravel x3 and iron sheet x2 each passed all 25 deployment and 16 execution checks with accepted backup, exact preview, bounded budgets and zero mutation/resource/session | PASS, IWP-04 |
| Visible gravel execution | Two nodes traversed BUILD/CONNECT/FEED/PROCESS/VERIFY; real Create machinery rotated; required/observed gravel 3/3 | PASS, IWP-06/IWP-07 |
| Visible iron-sheet execution | One belt/press node traversed every phase; the press visibly actuated; required/observed iron sheet 2/2 | PASS, IWP-06/IWP-07 |
| Journal cleanup | Gravel cleanup removed 36 owned blocks and zero unknown blocks. Belt cascade/runtime-`part` behavior is covered by two strict Forge unit tests; the two cells left by the pre-fix live run were explicitly cleared by the user | PASS with fixed live defect, IWP-08 |
| Contained C-03 water | Three orientations; 17 placements; local 125-cell scan finds exactly three intended water positions and zero outside | PASS, `create-processing-gametest-20260720-204753.log` |
| Goal-driven regression | Gravel/iron, three orientations, cancellation, rollback, duplicate-session, formal guard and four reload-stage cases | PASS, all 14 tests, `goal-driven-execution-gametest-20260722-085756.log` |
| Production C-03 recovery | Two JVMs; exact 17-position BUILD rescan; no repeated placement/input/output; independent stale-world refusal | PASS, `c03-recovery-reload-acceptance-20260720-205217.log` |
| Player BUILD reload/resume | Real iron-sheet x2 hold at BUILD, save-to-title/re-entry, `PERSISTED stage=BUILD`, exact rescan, no duplicate placement/consumption/output, then all five phases and 2/2 output | PASS, visible IWP-09/IWP-11 acceptance |
| CONNECT/PROCESS/VERIFY reload | CONNECT resumes from the pre-resource checkpoint; uncertain PROCESS refuses; VERIFY finalization is idempotent | PASS, dedicated 14-test GameTest matrix |
| Recovery identity mismatch | Changed region/world/fingerprint/owner/definition/journal refuses before mutation | PASS, core/Forge/GameTest fault matrix |
| Player cancel and cleanup | Real gravel x3 held at BUILD, cancelled, previewed and removed one owned change; persisted failed-session cleanup removed two owned cells; unknown removals 0 | PASS, visible IWP-10/IWP-11 acceptance |
| Final pure regression | 364 core + 38 Adapter API tests | PASS, `core-test-20260722-094129.log` |
| Final Forge unit regression | SavedData and cleanup/recovery integration | PASS, 7/7 tests |
| Final clean build | 23/23 tasks; 409 aggregate JVM tests; artifact isolation | PASS, `build-20260722-090116.log` |
| Final physical/reload regressions | C-02, C-03, C-04 and both two-JVM recovery fixtures | PASS, final `20260722-094324` through `20260722-094930` logs |
| Final isolated deployment | Built and deployed JAR SHA-256 `CE729F0C...B4800717`; zero Java/Minecraft processes and zero crash reports | PASS |
| Formal-world boundary | Formal `D:\PCL2` launcher count 0, write count 0; TEST_ONLY and formal execution structurally refuse | PASS, IWP-10 |

## Phase IV shared 3-5 Bot fleet WIP gate

| Scope | Evidence | Current result |
|---|---|---|
| Loader-neutral kernel | `GraphNeutralFleetCoordinatorTest`: exact 3/5 parallel assignment, exclusive leases, 2-5 bounds, exact snapshot/reconciliation and reassignment | PASS, 3/3 |
| Construction compatibility | `BotFleetExecutorTest`: existing public coordinator/executor contract, typed failure reassignment, persisted graph SHA-256 and completed-worker identity, cancel/reload/deadlock | PASS, 6/6 |
| Typed hand-off | C-10 mapping asserts `MATERIAL_DELIVERY` permits logistics-to-builder hand-off while carried reservations retain same-worker continuity | PASS |
| Forge source safety | Existing entity only; no ArmorStand/second scheduler, teleport, arbitrary use, player inventory or private container path | PASS |
| Physical 2 Bot | Existing Steve/Alex entity, parallel physical DAG, real withdrawal/transport/place, adjacent smooth movement and exact cleanup | PASS |
| Physical 3 Bot | Three real independent DAGs, maxActive 3, every worker moves, 3 sources/3 placements, overlap false, cleanup true | PASS |
| Physical 5 Bot | Five real independent DAGs, maxActive 5, every worker moves, 5 sources/5 placements, overlap false, cleanup true | PASS |
| Isolated aggregate | `scripts/Test-BotFleetGameTest.ps1`; Forge/Create 6.0.6, clean save/exit, crash reports 0, residual processes 0 | PASS, 5/5, `bot-fleet-gametest-20260727-171108.log` |
| Full JVM/Forge regression | `scripts/Test-Core.ps1`; `gradlew.bat :forge-create-1.20.1:test --no-daemon` | PASS after the composite wrapper-recovery change, JVM 531/531 across 125 suites and Forge 28/28; `core-test-20260727-172756.log` |
| Privacy/static boundary | Release privacy plus changed-line credentials/private/formal-path, pure-module imports, second-entity/scheduler, sleep and teleport scans | PASS; secrets/history/private names/large files all zero, only exact spawn uses `moveTo` |
| C-10 regression | Direct/Bots/Hybrid, real Deployer, refusal matrix, cancel, exact pre-resource reload and cleanup | PASS, 10/10, `c10-owned-workpiece-gametest-20260727-165222.log` |
| Site/integrated acceptance | Typed terrain adapter over this kernel, clean Site commits, integrated composites/fault/recovery and one visible pass | PENDING; no independent PCL2 run |

## Phase IV C-10 first-level Deployer gate

| Scope | Evidence | Current result |
|---|---|---|
| Typed interaction authority | Only `OWNED_DEPOT_ITEM` + `DOWN`; entity, combat, arbitrary block use, container, player inventory, private storage, unknown NBT and unknown side effects refuse independently | PASS, `DeployerInteractionPolicyTest` |
| Fixed physical plan | Motor + downward Deployer + owned Depot + output chest; exact interaction cell and rotated topology | PASS, `DeployerPlanTest` |
| Generic runner contract | Separate processed/held input boundaries; BUILD/POWER/FEED/PROCESS; exact held, cycle, consumed-input and chest-output evidence | PASS, `DeployerGenericExecutionPlanTest` |
| Loader-neutral evidence | Exact held before/after delta and every immediate safety signal are mandatory | PASS, `DeployerEvidenceTest` |
| Executor/semantics contract | Frozen third-party, Create capability execution and semantics contracts retain typed mode and held-item boundaries | PASS, 14 Adapter tests |
| Versioned handler safety | No fake-player, generic block-use, entity-interaction, teleport, fixed-sleep or formal-path call surface; safety configuration is present on the executable node | PASS, `CreateV606DeployerSafetyTest` |
| v606 geometry | Downward Y-axis Deployer, owned Depot, two ITEM inputs, one actual output and exact power route | PASS, `CreateV606MachineGeometryCatalogTest` |
| Compile | Core, Adapter, Forge main and test sources | PASS |
| Directed physical GameTest | Real `create:deploying/cogwheel` cycle and retained-tool candidate | PENDING shared C-06 through C-10 gate |
| Direct/Bots/Hybrid, fault, cancel, reload, cleanup | Common infrastructure and equivalence matrix | PENDING |
| Player-visible acceptance | One later Site-Preparation + production integrated run only | NOT RUN independently |

## Phase IV C-10 bounded world-workpiece expansion

| Scope | Evidence | Current result |
|---|---|---|
| Typed authority | Exact plan/session/region/workpiece/resource-buffer coordinates plus allowlisted stripped-log + andesite-alloy -> andesite-casing transition; every unrelated authority remains forbidden | PASS, 7 loader-neutral plan/policy tests plus 5 task-mapping tests |
| Versioned handler/backend/worker safety | Separate v606 handler, physical backend and Bot worker have no generic activate/use, player inventory, entity interaction, teleport, fixed sleep, material extraction/insertion or direct recipe world-mutation call path; reload admits only exact pre-resource build prefix; the worker reuses the existing entity/fleet boundary and cannot instantiate another scheduler | PASS, 5 Forge source-safety tests |
| Real Create cycle | Normal server ticks drive the owned downward Deployer; live `ITEM_APPLICATION` recipe, workpiece transition and held count `1->0` are observed | PASS |
| Container/entity/NBT safety | Adjacent BlockEntity/container, entity in activation volume and unknown held-item NBT each refuse before mutation; tagged source remains byte-semantically unchanged | PASS |
| Cancel, reload and cleanup | Pre-resource cancellation restores both machine cells, input and workpiece with zero warnings; exact two-entry build-prefix reload revalidates all identities/states and completes with input `1->0`, no duplicate placement/input; completed machine cleanup leaves the real casing | PASS |
| Isolated Forge aggregate | `scripts/Test-C10OwnedWorkpiece.ps1`; 10/10, clean save/exit, crash reports 0, residual processes 0 | PASS, `c10-owned-workpiece-gametest-20260727-165222.log` |
| Executor task mapping | Exact delivery -> safe interaction -> authoritative rescan graph; shared Direct/Bots/Hybrid declarations, reservations, REFUSE replay and full safety envelope | PASS, 5 focused mapping tests plus Direct/Bot/Hybrid and frozen Adapter contract suites |
| Direct physical backend | Shared `DirectWorldExecutor` runs all three tasks; delivery is readback-only, the real Deployer produces 3/3 application evidence, terminal rescan produces 2/2 evidence, mutations are edge-counted 3 world/1 material, and typed cancellation restores exact state | PASS, real GameTests |
| Bots physical backend | Existing Steve/Alex `ConstructionBotEntity` workers are selected by `BotFleetCoordinator` and executed by `BotFleetExecutor`; Steve owns one logistics task, Alex owns interaction/verification, both navigate through adjacent bounded safe cells, and exact output/cleanup pass without teleport or player inventory | PASS, real GameTest |
| Hybrid physical backend | Existing `HybridExecutor.safeDefaults` routes logistics to Steve/Bots and sensitive Create interaction plus system verification to Direct; all evidence retains exact physical-route provenance | PASS, real GameTest |
| Goal selection | Connect the mapped bounded world-workpiece route to C-10 production selection without inventing `VerifiedPhysicalPlan` provenance | WIP; no COMPLETE claim |

## RP-01 through RP-04 public runtime portability

| Scope | Evidence | Result |
|---|---|---|
| Forge config | Actual packaged JAR read `steve-industrial-agent-common.toml`; safe defaults remain write-disabled | PASS |
| World marker | Versioned SavedData binds world identity, generation, dimension policy and creation evidence across restart | PASS |
| Portable helper | PowerShell 7 and Windows PowerShell 5.1 synthetic tests; no overwrite/upload/formal write | PASS |
| Real backup | Stopped-game 18-file / 8,403,645-byte copy, SHA-256 manifest, equal source pre/post and restore drill | PASS |
| Restart verify | Second Forge process rehashed backup and restore trees and passed core `BackupVerifier` | PASS |
| Runtime independence | Fresh external root, no source/`work`/legacy IWP runtime dependency; important-root untouched | PASS |
| Build/regression | 23-task clean build; artifact isolation; zero crash reports | PASS, `build-20260722-122716.log` |

## IWP-05 isolated test-world backup

| Scope | Evidence | Result |
|---|---|---|
| Verifier-gated backup | Complete ordered SHA-256 manifest, capacity, isolated paths, staging and atomic publication | PASS, `BackupPlanVerifierTest` |
| Disposable restore drill | New sibling restore target exactly matches manifest; live source remains unchanged | PASS, `BackupPlanVerifierTest` |
| Formal hard stop | Formal/forbidden source and target fail before copy | PASS |
| Real closed-world run | `scripts/Test-IsolatedWritableWorldBackup.ps1`; 105 files/11,813,712 bytes; manifest `03d3438d...70f7`; exact restore drill | PASS, `iwp-world-backup-20260720-175527.log` |

## Phase IV-S typed terrain fleet

| Scope | Evidence | Result |
|---|---|---|
| Full regression | Integrated loader-neutral JVM regression | PASS, 550/550 across 129 suites, `core-test-20260728-160532.log`; final clean build 583/583 in `build-20260728-161519.log` |
| Terrain physical fleet | 8 GameTests: exact drops/salvage, 2/5 Bot, reload/rescan/reassignment, cancel, protected/stale refusal and prepared-site-to-real-C-07 hand-off | PASS with wrong-live-world refusal, `site-preparation-gametest-20260727-192618.log` |
| Construction fleet regression | Existing 2/3/5 Bot physical chain remains 5/5 | PASS, `bot-fleet-gametest-20260727-184847.log` |
| Player-visible integration | Combined terrain-to-construction scenario | Automated visible-equivalent registration PASS 22/22; actual backed-up new-region PCL2 run still pending and no independent PCL2 launch occurred |

## Phase IV-S explicit grading and destructive confirmation WIP

| Scope | Evidence | Result |
|---|---|---|
| Typed grading contract | Two corners, height, fill state, bounded cut/clear/fill sets, exact initial fingerprints, deterministic hash, defensive copies and action budget | PASS, focused `TerrainGradingSpecificationTest` plus site contract tests |
| Command surface and supply boundary | `corner1`, `corner2`, `height`, `supply`, `start`, `force-confirm`, `status`, `cancel`; fill rejects unsafe/non-item/block-entity defaults and uses only the exact dedicated chest | PASS, Forge command-tree/JVM tests and physical fixture |
| Safe slope/hole grading | Existing Steve/Alex fleet cuts high cells, clears the volume, consumes exact stone, fills holes bottom-up and leaves the exact requested plane; no teleport, overlap, player inventory or private-container access | PASS, Site Preparation 10/10, `site-preparation-gametest-20260730-203321.log` |
| Default high-risk refusal | Container/block entity, dangerous medium and unbreakable/protected cells produce warning evidence and no mutation without destructive authorization | PASS, contract/default protection fixtures |
| Exact destructive confirmation | Second scan binds warning hash, player, expiry, grading hash, effective snapshot and exact risk set; mismatched/stale authorization refuses | PASS, loader-neutral authorization tests and physical second-confirmation fixture |
| Forced physical removal | Confirmed chest-with-item, TNT and bedrock are removed without reading/dropping private contents; exact counters are nonzero and final surface/cleanup pass | PASS after fixing ordinary chest-removal content drops. Honest failing 9/10: `site-preparation-gametest-20260730-202524.log`; fixed 10/10: `site-preparation-gametest-20260730-203321.log` |
| Integrated regression | Site plus C-05-C-10 Direct/Bots/Hybrid, Composite-01/02/03 and 3/5 Bot | PASS 22/22 with crash reports/residual processes 0, `phase-iv-integrated-visible-gametest-20260730-205729.log`; player-visible PCL2 gate still pending |

## macOS C-08 HEATED metadata and fuel-reservation gate

| Scope | Evidence | Result |
|---|---|---|
| Runtime metadata | NONE/HEATED/SUPERHEATED/unsupported tiers, exact recipe/capability provenance, continuous requirement, preflight/evidence/diagnostics and optional typed fuel survive catalog resolution without changing the frozen graph contracts | PASS, core/Adapter unit tests |
| Bound sidecar | Same planning/binding/physical/runtime/session/step identities, exact Basin/Mixer implementation, owned Blaze Burner and one-execution limit are checked before materialization | PASS, full JVM regression |
| Fuel reservation | One ordinary coal is reserved exclusively from the exact plan-owned buffer; a competing root session refuses, cancel releases, and no player/arbitrary-container path exists | PASS, physical C-08 GameTest |
| Real HEATED equivalence | `create:mixing/brass_ingot`, copper x1 + zinc x1 + coal x1, actual Create Burner insertion and live `KINDLED` evidence | PASS across Direct/Bots/Hybrid inside one GameTest |
| Reload boundary | BUILD checkpoint releases unused coal, exact rescan/replan reacquires it, then produces brass once; resource-bearing/consumed-fuel recovery remains refused | PASS, physical C-08 GameTest |
| Targeted C-08 aggregate | Three registered tests, clean world save/shutdown | PASS 3/3, `macos-c08-heated-gametest-final.log` |
| Full JVM/clean build | Core 498, Adapter API 56, Forge 35; all 23 tasks executed | PASS 589/589, `macos-clean-build-c08-heated-final.log` |
| Integrated regression | Existing Site, C-05-C-10, Composite and fleet matrix on a fresh flat isolated world | PASS 22/22, `macos-phase-iv-integrated-visible-c08-heated-final.log` |
| Platform boundary | No PCL2 launch, formal save, release install/publish or Windows acceptance | `MAC_AUTOMATION_COMPLETE / WINDOWS_VALIDATION_DEFERRED` |
