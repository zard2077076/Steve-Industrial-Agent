package dev.stevecreate.agent.core.player;

/** Player-facing selection of an existing bounded execution strategy. */
public enum PlayerExecutionMode {
    SMART_RECOMMENDED,
    DIRECT,
    BOTS,
    HYBRID
}
