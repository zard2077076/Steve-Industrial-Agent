package dev.stevecreate.agent.core.deployment;

/** PW-05 risk input only; PW-10 owns the final claim Adapter result. */
public enum DeploymentPermissionRisk {
    ALLOWED,
    UNKNOWN,
    DENIED,
    NOT_INSTALLED
}
