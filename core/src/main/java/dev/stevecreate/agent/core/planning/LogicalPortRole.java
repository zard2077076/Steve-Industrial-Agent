package dev.stevecreate.agent.core.planning;

/** Semantic role of a typed logical port requirement. */
public enum LogicalPortRole {
    PROCESS_INPUT,
    PROCESS_OUTPUT,
    EXTERNAL_SUPPLY,
    TARGET_DEMAND
}
