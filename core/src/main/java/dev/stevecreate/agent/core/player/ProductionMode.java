package dev.stevecreate.agent.core.player;

/** Continuous inventory control is modeled but deliberately unavailable in Phase IV-C. */
public enum ProductionMode {
    ONCE,
    MAINTAIN_STOCK_UNAVAILABLE
}
