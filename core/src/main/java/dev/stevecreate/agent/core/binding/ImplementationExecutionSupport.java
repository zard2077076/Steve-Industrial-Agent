package dev.stevecreate.agent.core.binding;

/** Evidence level of the implementation itself; this is not execution authority. */
public enum ImplementationExecutionSupport {
    DESCRIBED_ONLY,
    PHYSICALLY_VERIFIED
}
