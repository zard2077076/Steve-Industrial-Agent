package dev.stevecreate.agent.core.deployment;

/** Offline occupancy evidence used before backup and live pilot operations. */
public enum WritableTestWorldOccupancy {
    CLOSED,
    IN_USE,
    UNKNOWN
}
