# Changelog

- Completed PW-12 with typed authoritative `/industrialagent deploy preview|risks|budget|readiness` dry-run commands, deterministic hash/environment/bounds/material/power/risk/gate output, invalid-input and wrong-thread refusal, real Create-only and disposable DeceasedCraft custom-chain acceptance, and structural proof of zero formal/session/resource/machine/world/LLM authority.
## Unreleased

- Stabilized the shared C-03/C-06--C-10 player-acceptance path by waiting for
  teleport synchronization before fixture inspection, moving the salvage fixture
  outside the reviewed construction/clearance footprint, and keeping the
  demolition-approval controls visible in short GUI windows. Focused Harness
  tests and the full offline Gradle test/build pass; C-06 real-client completion
  remains unverified.
- Added the Phase IV-C player-workflow foundation: a craftable Steve Engineer
  Terminal, bilingual searchable 11-goal picker, server-issued expiring nonces,
  world-local project persistence, four-direction ghost placement and three
  honest layout policies. This remains Mac development work and is not a
  release or formal-world authorization.
- Added tick-sliced smart relocation over exactly 216 candidates (nine nearby
  anchors, two elevations, four rotations and three layouts), fail-closed
  hard-conflict ranking, exact clearance observation, progress UI and stale
  candidate rescan before selection.
- Added a one-page clearing confirmation backed by a fresh authoritative
  survey. Protected blocks, containers/data, unknown blocks and hazards disable
  approval. Issued two-minute tokens persist exact obstacle states and bind the
  project, target, quantity, anchor, orientation, layout, region hash, safety
  policy, player, world, dimension, plan/snapshot hashes and execution mode.
- Added the WIP player bridge to the existing Site Preparation Bot executor:
  explicit dedicated salvage-container targeting, exact pre-start revalidation,
  a short start window, real Bot path/mine/drop/collect/deliver behavior,
  location-only persistence, safe pause on container/capacity loss and an
  all-stacks capacity preflight that prevents partial delivery.
- Added the packet-throttled project HUD and Manage Project screen with
  evidence-only counts, pause/resume/safe-cancel controls and durable resume
  routing for placement, survey and approval. A full process restart now marks
  the project paused and requires fresh authorization instead of reporting a
  false executor resume. Construction remains stopped at the explicit
  player-material-source boundary.

- Completed the automated Phase IV C-05 checkpoint: one verified opposed
  crushing-wheel plan now executes real gravel-to-sand processing through
  Direct, two active Bots and Hybrid, with live kinetic/controller/recipe/input/
  chest-output evidence, optional byproduct observation, typed runtime faults,
  cancellation cleanup and exact mid-BUILD recovery.
- Added standard and disposable DeceasedCraft crushing recipe discovery. The
  pack probe selects a safe raw-copper recipe without creating an execution
  session, reading a save or mutating the external PCL2 instance.

## 0.1.0-alpha.1 - Release candidate preparation

- Connected production Forge config to setup, region, readiness, dry-run and every exact
  Create mutation guard; normal players no longer need repository IWP JVM properties.
- Added versioned per-world test markers and `/industrialagent setup` status/mark/unmark/
  validate/show-config commands.
- Added `/industrialagent backup` status/prepare/verify/list/explain plus a bundled
  stopped-game PowerShell helper with non-overwriting SHA-256 backup, source pre/post
  fingerprints, restore drill, completion marker and restarted JAR verification.
- Passed the packaged JAR config/marker/backup/restart chain outside the repository with
  no `work` runtime dependency, important-root write or crash report.

- Prepared the first playable writable-world Alpha boundary for disposable
  creative test worlds, limited to gravel x3 and iron sheet x2.
- Added repeatable privacy/history scanning, portable configuration precedence,
  formal-root isolation, deterministic build metadata and distribution checks.
- Added public installation, quick-start, command, compatibility, security and
  future-version documentation without publishing any remote release.

- Added IWP-04 through IWP-08 player-world pilot commands for 25-check
  readiness, zero-mutation dry-run, visible staged execution, bounded TEST_ONLY
  inputs/power, status/cancel and journal-owned cleanup. The accepted isolated
  client run produced gravel 3/3 and iron sheets 2/2 through real Create
  machinery.
- Contained the C-03 water column with a 17-placement typed structure and a
  local 125-cell GameTest scan that permits only the three intended water cells.
  Hardened cleanup for Create's runtime-managed belt `part` transition and for
  adjacent-belt cascade removal while continuing to refuse geometry, facing,
  slope or unrelated property drift.
- Bound the launcher to the accepted IWP-05 backup pointer and added exact
  world/dimension lifecycle checks that remain usable after session-owned world
  changes. Player-command checkpoint persistence and reload/resume remain
  deliberately unavailable pending IWP-09 acceptance.
- Added IWP-01 through IWP-03: exact isolated writable test-world discovery,
  full typed refusal vocabulary, formal/backup hard exclusions, player/session
  bound region selection, tick-sliced zero-block particle preview and confirm,
  plus an account-free repository-owned DeceasedCraft player instance builder,
  launch plan and offline world-marker tool.
- Fixed the standalone IWP client launcher for Windows PowerShell 5.1 by replacing
  the unavailable `ProcessStartInfo.ArgumentList` collection with safely encoded
  Windows arguments and adding a zero-process compatibility regression.

- Completed FB-07 with one guarded real external backup, canonical persisted manifest/completion identity, independent full-tree verification, disposable exact-identity restore drill, equal formal-source fingerprints and zero write/session/private-semantic-read/game-process evidence.
- Completed FB-08 with aggregate-only backup/restore documentation, eight pending preview-unavailable candidate packages, eight awaiting-selection approval requests, zero formal allowed decisions, final physical/runtime regressions and an unchanged unconditional formal execution hard stop.

- Added FS-01 bounded formal-save discovery models and typed failures: only direct ordinary save candidates and `level.dat`/`level.dat_old` metadata are eligible, while multiple candidates remain explicitly ambiguous before any region read.
- Added FS-02's callback-scoped READ-only formal file guard, fail-closed write/lock/process/private/reparse checks, repository-only JSONL audit and deterministic metadata/key-hash world fingerprints.
- Added FS-03 loader-neutral formal survey, finding, evidence, limitation and candidate-zone models with complete provenance, fixed offline confidence, pending-only selection and structural privacy/write/execution refusal.
- Added FS-04's guard-backed bounded gzip NBT and streaming Anvil parser with sector/compression/size/depth/version validation, corrupt-chunk isolation, loader-neutral palette output and parse-time removal of player/entity/inventory/container contents.
- Added FS-05's single-thread atomic survey budget and deterministic metadata/hot-sample/neighbor/candidate-deep scheduler with seven explicit limits, bounded inputs, duplicate suppression and typed partial termination at the first exhausted dimension.
- Hardened FS-01/FS-02 before live use so the read-only guard alone performs bounded non-recursive direct save enumeration and metadata lookup; `FormalSaveDiscovery` now has no filesystem API and nested region data cannot be observed before unique selection.
- Corrected FS-04 block-state decoding to Minecraft 1.20.1's non-crossing values-per-long BitStorage layout, added bounded local lookup and rejected packed indices outside the declared palette before classification.
- Completed FS-06 loader-neutral offline Create/storage classification with exact block provenance, all required infrastructure families, generic Create retention, presence-only known/heuristic storage, candidate membership and structural refusal of contents/live runtime claims.
- Completed FS-07 deterministic offline rotational-power, ITEM and fluid topology approximation with persisted-axis confidence, explicit partial-coverage boundaries, cross-region provenance and structural refusal of runtime connectivity claims.
- Completed FS-08 fixed-weight deterministic candidate-zone scoring with evidence-derived infrastructure distance, canonical identities/ties, explicit unknown/protected authorization requirements and pending-only output.
- Completed FS-09 allowlisted claim-mod metadata discovery with version/hint evidence, credential/database/path refusal, UNKNOWN permission invariants and Adapter-backlog-only output.
- Completed FS-10 exact four-member formal dry-run batch validation with pending-zone anchor binding, valid shared-snapshot artifacts and structural execution refusal.
- Completed FS-11 registered fixed-token formal survey commands with typed path/identity refusal, deterministic ignored report keys and zero side-effect authority.
- Hardened the FS-12 pre/post fingerprint so guard-owned metadata covers every ordinary file, all non-private content can be SHA-256 hashed, and playerdata/stats/advancements are never opened.
- Completed the real FS-12 bounded formal-world survey with equal source/external fingerprints, 9,733 parsed chunks, machine-only candidate seeding, eight pending UNKNOWN candidates, four explicitly blocked dry-runs, zero private opens/writes/sessions/crashes/processes and full physical regression.
- Added FB-01's formal-backup destination policy with exact identity/capacity binding, repository-ignored outside-formal targets, reparse/collision/space refusals and zero copy/write/cloud/overwrite authority.
- Added FB-02's formal-source backup transaction with mandatory quiescence evidence, opaque private-file transport, fixed lock/temp exclusions, per-file path/size/mtime/SHA-256 manifests, verified unique staging, atomic publication and backup-only completion markers.
- Added FB-03's persisted canonical manifest/completion identity, independent completed-tree verification, bounded retention evidence and disposable repository-work restore drill with exact file rehash plus offline `level.dat` WorldIdentity reparse.
- Added FB-04 deterministic formal deployment candidate packages that bind complete available preview/physical/risk/budget evidence or explicit unavailable blockers, retain UNKNOWN claims and user-selection requirements, and make ranking incapable of auto-selection, approval or execution.
- Added FB-05 awaiting-selection and exact one-time approval-request models, restore-copy-only TEST_ONLY decisions and an unconditional formal-world execution hard stop; formal decisions remain absent.
- Added FB-06's exact seven-command registered-identity grammar, audit-only zero-authority command plans, request-only bounded inventory authorization and UNKNOWN claim readiness refusal.
- Added PW-11's independent twenty-five-check deployment-readiness aggregation, exact typed refusal context and isolated-only data-only `DeploymentReadyPlan`, with complete mutation/replay/reflection coverage and fresh standard/DeceasedCraft physical regressions.
- Added PW-01 loader-neutral canonical world-environment classification with explicit root policy, identity/fingerprint provenance, traversal and symlink defenses, fail-closed unknown/mismatch behavior and non-writable/non-executable forbidden/formal results.
- Added PW-02 immutable deployment policy with bounded environment/root/capability/time/material/power/mutation constraints and a permanently execution-disabled, zero-mutation formal-world default.
- Added PW-03 formal-world write protection across seven typed entry classes and every existing Create v606 handler, route, resource and rollback write boundary, with a live post-session revocation GameTest.
- Added PW-04 deterministic loader-neutral deployment previews with complete physical-chain projection, canonical JSON, self-validating SHA-256, explicit snapshot/runtime identity, construction BOM, route/power/risk/policy evidence and no execution authority.
- Added PW-05 deterministic typed deployment risk assessment across 22 required categories and five fixed severities, with every CRITICAL finding blocking approval by construction.
- Added PW-06 immutable read-only deployment budgets for raw/intermediate/construction/logistics/power/input/output requirements, stress/capacity/tick/mutation/rollback/journal bounds and explicit resource-source violations without reservation or withdrawal authority.
- Added PW-07 exact loader-neutral region authorization evidence and typed scope checks for world, dimension, owner, bounds, operations, mutations, preview/snapshot/runtime identity, expiration, one-time use, approval, revocation and provenance, while keeping formal-world live checks impossible.
- Added PW-08 structured one-time human approval tokens and an atomic consume gate bound to exact preview, environment/world, snapshot/runtime/reload, region, target/quantity, mutation budget, deployment policy and expiry; only explicit isolated `TEST_ONLY` approvals are constructible for tests.
- Added PW-09 isolated-only backup planning, canonical file inventory and SHA-256 manifests, capacity/target/atomicity/consistency/restore/retention/failure/approval verification and a real temporary-world backup-modify-restore fingerprint drill with typed incomplete/space/path/journal refusal.
- Added PW-10 loader-neutral permission query/decision/evidence plus an open `ClaimAdapter` boundary with isolated allow, explicit deny, unknown, not-installed and fake third-party JVM proofs; formal/UNKNOWN construction remains denied and detected OPAC is deferred without API integration.

## 0.1.0-SNAPSHOT - 2026-07-13

- Initialized the isolated Steve Industrial Agent Git/Gradle project.
- Added loader-neutral core and adapter API modules.
- Added bounded registry scans for Create and optional Mekanism detection.
- Added deterministic no-power and stress-overload diagnostics with tests.
- Recorded the installed Forge/Create compatibility fingerprint and absent Mekanism state.
- Added project governance, scripts, safety boundaries, and long-term Create/Mekanism backlog.
- Added C-01 bounded Forge registry scanning with real Create block and unloaded-chunk evidence.
- Added C-02 strict Create 6.0.6 kinetic telemetry for stopped, signed powered and overstressed networks with canonical replay.
- Added C-03 typed water-wheel/millstone construction and a real cobblestone-to-gravel GameTest loop.
- Added C-04 typed belt/press/funnel/chest construction and a real iron-ingot-to-iron-sheet GameTest loop.
- Added T-02 neither-mod and Create-only dedicated-server runtime profiles with typed optional-mod outcomes.
- Added T-02A bounded `neither-client` userdev acceptance through a real rendered screen, typed Create/Mekanism absence, automatic clean shutdown and strict log/crash checks.
- Added T-02B bounded `create-only-client` userdev acceptance with actual Create 6.0.6-150 initialization, typed Create success/Mekanism absence, automatic clean shutdown and full C-02/C-03/C-04 regression.
- Added G-01 loader-neutral generic resource categories with stable serialized identities and pure JVM coverage.
- Added G-02 immutable bounded machine nodes, ports, edges and a loader-neutral unified graph, with strict invariants and pure JVM C-03/C-04 topology fixtures.
- Added G-03 bounded generic process specifications for resources, recipes, capabilities, evidence requirements and verification modes while preserving separate millstone and belt-press contracts.
- Added G-04 sealed bounded generic execution-step descriptors with typed handlers, conditions, retry, cancellation, rollback and evidence requirements but no executable callbacks.
- Added G-05 immutable in-memory generic plans and sessions with monotonic progress, bounded references, allowlisted retry and explicit cancellation/failure/completion states.
- Added G-06 a Create-free bounded step runner with one action invocation per tick and fake-handler coverage for completion, failure, timeout, cancellation, verification and retry exhaustion.
- Added G-07 complete immutable verification evidence for eleven stable kinds and upgraded session/runner gating to require passing current-step observations with values.
- Added G-08 bounded generic verification rules with required/optional evidence, latest-observation evaluation, typed failure/timeout results, adapter-specific permission and guards that preserve every C-03/C-04 process evidence requirement.
- Migrated the fixed C-03 production flow to its relative unified machine graph, four generic steps, shared session/runner/rules and an exact Create 6.0.6 action handler while preserving the real cobblestone-to-gravel GameTest contract.
- Migrated the fixed C-04 production flow to its relative unified machine graph, four generic steps, shared session/runner/rules and an exact Create 6.0.6 action handler while preserving real belt transport, the observed 240-tick press cycle and iron-sheet chest output.
- Added G-10 bounded world-change journals with full block snapshots, optional block-entity data, injected-resource and irreversible-processing records, typed cancellation results and conservative no-compensation rollback; C-03/C-04 now prove early-construction cancellation and exact safe restoration before retaining their full physical processing acceptance.
- Added G-11 versioned bounded recovery checkpoints for canonical plan/graph fingerprints, complete session and journal state, trusted-plan matching, mandatory exact world rescan and typed stale-session refusal. Its original two-process Minecraft `SavedData` fixture proves minimal consistent-state resume without blind execution; production handler reload was explicitly deferred from that milestone.
- Added G-12 test-source-only virtual industrial adapter coverage proving that the unchanged loader-neutral graph, process, step, session, bounded runner and verification rule support both successful ITEM processing evidence and typed action failure without Create inheritance or game/mod imports; no real third-party integration is claimed.
- Added G-13 typed anchors and whole-plan 0/90/180/270-degree Y transforms for coordinates, block and machine orientation and port sides while preserving graph identities and edges. C-03 and C-04 now each pass real Create processing at zero plus two non-default orientations; mirroring, feasibility search and automatic layout remain out of scope.
- Added G-14 bounded loader-neutral basic placement feasibility with complete typed conflict reports for unloaded, non-replaceable, protected and internally conflicting final roles. C-03/C-04 now reject infeasible transformed layouts before session creation or mutation, while retaining all three-orientation real processing acceptances in deterministic repository flat-world fixtures; alternative-layout search remains out of scope.
- Added a bounded packaged-production-JAR neither-mod dedicated-server acceptance. The clean-built single release JAR loads as the only mod under official Forge 47.4.10 without Create or Mekanism, returns both typed absences, saves and exits cleanly, and passes strict dependency-entry, class-loading, Mixin, thread, error and crash scans.
- Added a bounded two-process production C-03 save/reload acceptance. It checkpoints only after all ten BUILD changes and before resource injection, proves the raw Create 6.0.6 transient state fails closed, narrowly canonicalizes `NeedsSpeedUpdate` on four trusted live kinetic snapshots, proves typed stale refusal without mutation, restores the v606 handler/step cursor without repeated placement or feed, then completes the real cobblestone-to-gravel recipe with exact journal cardinality and zero new crash/error markers.
- Added a bounded two-process production C-04 save/reload acceptance. It checkpoints only after all eight BUILD actions and before input, narrowly canonicalizes Create 6.0.6's proven transient `NeedsSpeedUpdate` byte on six live kinetic snapshots, retains exact comparison for every other field, proves typed stale refusal, restores handler/step cursors without duplicate placement/input/output, and completes one real 240-tick iron-ingot pressing cycle with zero new crash/error markers.
- Closed every currently executable T-02 stability row with a final 115-test JVM suite, 21-task clean build, fresh C-02 telemetry, three-orientation C-03/C-04 physical processing, two-process production reloads, packaged neither-mod startup and strict zero-error/crash scans. Mekanism-only and dual-mod rows remain explicitly blocked on an official dependency pin.
- Froze the accepted foundation as annotated tag `generic-foundation-v1` at `340d587edfe684299aa8a21ff7159f0d5d01702e` and opened the read-only Goal-Driven Planning Foundation backlog P-02 through P-10.
- Added P-02 immutable loader-neutral `ProductionGoal`, bounded material/mod/depth/strategy/owned-resource constraints and a versioned canonical CRC-protected codec with fail-closed validation.
- Added P-03 immutable loader-neutral recipe descriptions and a deterministic supplied `RecipeCatalog`, including typed quantities/byproducts, capability/resource requirements, duration and explicit adapter/mod/runtime attribution; no runtime Create recipe discovery was added.
- Added P-04 immutable loader-neutral machine-capability declarations and deterministic catalog queries for recipe types, typed ports, power/resources, actions, evidence, diagnostics and Adapter-owned opaque version limits; no Create runtime mapping was added.
- Added P-05 bounded deterministic process dependency expansion for single/multi-step and multi-input test-catalog recipes, including quantities, intermediate edges, alternatives, raw/owned resources, byproducts, cycle/depth/constraint failures and fresh C-02/C-03/C-04 physical regression proof.
- Completed P-06's explicit typed planning-failure contract with exactly eleven codes, bounded location/nonempty trace/reason/alternatives and sealed nonempty-success versus typed-failure results.
- Added P-07 deterministic coordinate-free candidate generation with selected recipes/capabilities, materials/intermediates, scaled order/dependencies, unbound logical machine nodes/layout, planning evidence, estimated time and layout-independent SHA-256 identity.
- Added P-08 explicit deterministic lower-is-better scoring with complete weighted contributions, unavailable-capability and ordered-mod preference, integer owned utilization, time/unknown-time handling, goal-strategy multiplier, BigInteger overflow safety and stable ID tie-breaking.
- Added P-09 bounded loader-neutral `LogicalMachineGraph` values and deterministic candidate mapping with exact resource identities, raw/owned/intermediate/target flows, explicit capability power requirements and staged typed failures. Implementation binding and physical layout/materialization remain separate; frozen `UnifiedMachineGraph` and C-03/C-04 execution semantics are unchanged.
- Completed P-10 with a read-only eight-check `PlanningVerifier`, ten typed rejection codes and a non-publicly-constructible, non-executable `VerifiedLogicalPlan` success value. The full P-02 through P-10 supplied-catalog planning pipeline is deterministic and world-free; runtime recipe discovery, implementation binding, layout and execution remain future work.
- Added R-01's exact Forge 1.20.1/Create 6.0.6 `CreateRuntimeRecipeCatalog` boundary with authoritative server-thread `RecipeManager` enumeration, deterministic version/world/reload/content fingerprints, one-world loader-neutral snapshot caching, datapack reload invalidation, server-stop cleanup and complete typed runtime-knowledge failures. Its isolated server proof observed 2,609 live recipes and intentionally returned `RECIPE_MAPPING_FAILED` until R-02 adds milling/pressing translators; no fixed or empty catalog is presented as runtime truth.
- Added R-02 runtime milling/pressing translation plus the additive unresolved ingredient-entry foundation: exact, stable any-of, fingerprinted tag and explicit unsupported-complex identities never leak Minecraft objects or alter frozen `CatalogRecipe`. The isolated Create-only server accounted for all 44 milling and 7 pressing entries as 32 mapped or 19 typed output limitations, retained 18 non-fatal secondary-output warnings, preserved `forge:ingots/iron`, rebuilt after reload and made no world change.
- Completed R-03 with a bounded canonical CRC-protected ingredient codec and a request-specific resolver that prefers owned candidates deterministically, filters forbidden resources, retains original runtime recipe/tag/selection provenance and returns typed unresolved or stale-fingerprint limitations. The 193-test JVM suite, clean build and fresh C-02/C-03/C-04 physical regressions all pass.
- Completed R-04 with a runtime-attributed, loader-neutral Create 6.0.6 capability catalog for milling and pressing. Both capabilities declare ITEM ports, continuous rotational power, diagnostics/evidence, exact version/reload fingerprint and recipe-scoped C-03/C-04 physical proof without coordinates, orientation, implementation binding or sessions; 195 JVM tests, clean build, real-server reload proof and fresh C-02/C-03/C-04 regressions pass.
- Completed R-05 by connecting matching runtime recipe/capability snapshots to the existing deterministic dependency, candidate, scoring, logical graph and verifier pipeline. The real server proves a two-step andesite→cobblestone→gravel plan and iron-sheet pressing with real IDs, tag evidence, quantities and fingerprint; 199 JVM tests, clean build and fresh C-02/C-03/C-04 regressions pass without adding physical authority.
- Completed R-06 with the server-authoritative `/industrialagent plan create <resource_id> <quantity>` command, bounded loader-neutral presentation and structured trace. Real success, typed missing-recipe failure and parser rejection of invalid ID/quantity all pass without world mutation, sessions, executors or LLM access; 199 JVM tests and the clean build remain green.
- Completed R-07 acceptance with exact accounting of the live Create-only recipe manager, deterministic verified gravel/iron-sheet plans, reload fingerprint replacement, server-thread enforcement, static exclusion of physical authority and a packaged no-Create command proof returning `REQUIRED_MOD_UNAVAILABLE`. The 199-test JVM suite and 21-task clean build pass; accepted runs contain zero world/session mutation, strict errors or crash reports.
- Completed R-08 with a repeatable metadata-fingerprinted read-only audit of the formal DeceasedCraft client/server inputs. The pack matches Minecraft 1.20.1/Create 6.0.6 but differs in Forge patch, mod set and KubeJS recipe generation, so its runtime counts remain explicitly unverified; R-09 isolated-pack-profile is planned and no external file, save or world was read or changed.
- Closed Create Runtime Knowledge Foundation with a final 199-test JVM suite, 21-task clean build, fresh C-02/C-03/C-04 physical regressions and final real RecipeManager planning/reload/command acceptance. Strict combined scans report zero relevant errors or crash reports; runtime logical plans remain coordinate-, orientation-, implementation- and execution-free.
- Completed Implementation Binding Foundation with exact runtime-fingerprinted millstone/press declarations, deterministic logical-node binding, preserved runtime Ingredient selections, fifteen independent verification checks and twenty typed failures. Only a non-executable `VerifiedImplementationBoundPlan` crosses the gate; layout and execution authority remain separate.
- Completed Layout and Physicalization Foundation with immutable geometry/port/clearance/snapshot constraints, deterministic bounded placement and ITEM routing, descriptor-derived rotational-power/stress validation, strict unified-graph materialization and an independent thirteen-check `VerifiedPhysicalPlan` gate. Standard Create and isolated DeceasedCraft profiles prove three orientations, deterministic replay and typed failures without construction, sessions or external-world mutation.
- Completed Execution Integration with a sixteen-check independent readiness gate, exact v606 verified-plan adaptation, journaled physical ITEM routes, real chest-backed resources, bounded phased root control, live target/power/output validation, cancellation/rollback, safe BUILD reload resume and unsafe resource-history refusal. Eleven real GameTests cover standard multi-quantity milling/pressing and the complete negative matrix while all 232 JVM/Forge tests, 23 clean-build tasks and C-02/C-03/C-04 regressions remain green.
- Completed the Controlled End-to-End Production Pilot. Standard gravel x3 (three orientations) and iron sheet x2 remain green; the production JAR in the disposable 288-mod DeceasedCraft profile produced `immersiveengineering:dust_coke x4` and tag-backed `apocalypsenow:can x3` through live KubeJS/Create recipes, exact 8/15/13/16 verifier gates, real construction/input/processing/output, bounded journals/traces and cleanup. Final tests report 232 passing tests, a 23-task clean build, fresh C-02/C-03/C-04, zero crash reports/residual processes, unchanged external fingerprints and an unmoved `generic-foundation-v1` tag.
